* NULL

name=NULL
layer=character-set

@en
There is a <code class="charname">NULL</code> character
@ja
<code class="charname">NULL</code> 文字があります

@en

<p>The <code>U+0000</code> <code class="charname">NULL</code>
character must not be used in a document.

@ja

<p><code>U+0000</code> <code class="charname">NULL</code> 
文字を使ってはいけません。</p>

* parser:NULL

name=in-body-null
name=in-foreign-content-null
name=in-select-null
name=in-table-text-null
name=in-element-null

@en
The <code>NULL</code> character is ignored
@ja
<code>NULL</code> 文字は無視されます

@en

<p>The <code>U+0000</code> <code class="charname">NULL</code>
character must not be used in a document.  The <code
class=charname>NULL</code> character in an element is ignored.

@ja

<p><code>U+0000</code> <code class="charname">NULL</code> 
文字を使ってはいけません。要素内の <code class=charname>NULL</code>
文字は無視されます。

!!!
<!DOCTYPE HTML><body>$$\u0000
!!!
<!DOCTYPE HTML><body><svg>$$\u0000
!!!
<!DOCTYPE HTML><select>$$\u0000
!!!
<!DOCTYPE HTML><table>$$\u0000

* parser:EOF

name=EOF

@en
Unexpected end of file in a tag or declaration
@ja
タグや宣言の途中でファイルが途切れています

@en

<p>There is an end-of-file within a tag, declaration, or section.

@ja

<p>タグや宣言、区間の途中でファイルが終わっています。

* stray end tag

name=after-body-end-html
name=after-head-end-else
name=before-head-end-else
name=before-html-end-else
name=in-body-end-a3bbcd5f4hhlmmnopssu
name=in-body-end-applet-marquee-object
name=in-body-end-body
name=in-body-end-dd-dt
name=in-body-end-form
name=in-body-end-form-3
name=in-body-end-h6
name=in-body-end-html
name=in-body-end-else-2
name=in-body-end-li
name=in-caption-end-bccht6
name=in-caption-end-caption
name=in-caption-end-table
name=in-cell-end-bc3h
name=in-cell-end-t5
name=in-cell-end-td-th
name=in-column-group-end-col
name=in-column-group-end-colgroup
name=in-frameset-end-frameset
name=in-head-end-else
name=in-head-end-template
name=in-head-noscript-end-else
name=in-row-end-bc3htt
name=in-row-end-table
name=in-row-end-tbody-tfoot-thead
name=in-row-end-tr
name=in-table-body-end-table
name=in-select-end-select
name=in-select-end-option
name=in-select-end-optgroup
name=in-table-body-end-bc3ht3
name=in-table-body-end-tbody-tfoot-thead
name=in-table-end-bc3ht6
name=in-table-end-table
name=in-template-end-else
name=in-element-end-else-3
name=in-element-end-else
name=before-doctype-end-else
name=before-root-element-end-else
name=after-root-element-end-else
value=tag_name

@en
The tag <code>&lt;/<var>{value}</var>></code> has no corresponding
start tag
@ja
終了タグ <code>&lt;/<var>{value}</var>></code>
に対応する開始タグがありません

@en

<p>The end tag has no corresponding start tag.  There must be a start
tag with the same tag name before the end tag.  The end tag is
ignored.</p>

<p>Note that void elements have no end tag, that is, following
elements cannot have end tags: <code>area</code>, <code>base</code>,
<code>br</code>, <code>col</code>, <code>embed</code>,
<code>hr</code>, <code>img</code>, <code>input</code>,
<code>keygen</code>, <code>link</code>, <code>menuitem</code>,
<code>meta</code>, <code>param</code>, <code>source</code>,
<code>track</code>, and <code>wbr</code>.</p>

<p>The <code>html</code> and <code>html</code> elements cannot be
closed in a table.  Such an end tag is ignored.

@ja

<p>この終了タグには対応する開始タグがありません。
終了タグより前に同じタグ名の開始タグが必要です。終了タグは無視されます。</p>

<p>なお void 要素は終了タグを持ちませんから、次の要素では終了タグを使えません:
<code>area</code>, <code>base</code>, <code>br</code>,
<code>col</code>, <code>embed</code>, <code>hr</code>,
<code>img</code>, <code>input</code>, <code>keygen</code>,
<code>link</code>, <code>menuitem</code>, <code>meta</code>,
<code>param</code>, <code>source</code>, <code>track</code>,
<code>wbr</code>。</p>

<p>表の中では <code>html</code> 要素や <code>body</code>
要素を閉じることはできません。そのような終了タグがあっても無視されます。

!!!
<!DOCTYPE html>$$</hoge>
!value=hoge
!!!
<!DOCTYPE html><html>$$</hoge>
!value=hoge
!!!
<!DOCTYPE html></head>$$</hoge>
!value=hoge
!!!
!context=html
<!DOCTYPE html></body>$$</html>
!value=html
!!!
<!DOCTYPE HTML><body>$$</div>
!value=div
!!!
<!DOCTYPE HTML><body>$$</object>
!value=object
!!!
!context=body
$$</body>
!value=body
!!!
<!DOCTYPE HTML><body>$$</dt>
!value=dt
!!!
<!DOCTYPE HTML><body>$$</form>
!value=form
!!!
<!DOCTYPE HTML><body><template><br>$$</form>
!value=form
!!!
<!DOCTYPE HTML><body>$$</h4>
!value=h4
!!!
!context=body
$$</html>
!value=html
!!!
<!DOCTYPE HTML><body>$$</hoge>
!value=hoge
!!!
<!DOCTYPE HTML><body>$$</li>
!value=li
!!!
<!DOCTYPE HTML><table><caption>$$</html>
!value=html
!!!
<!DOCTYPE HTML><table><caption>$$</body>
!value=body
!!!
<!DOCTYPE HTML><table><caption>$$</thead>
!value=thead
!!!
<!DOCTYPE HTML><table><caption>$$</tbody>
!value=tbody
!!!
<!DOCTYPE HTML><table><caption>$$</tfoot>
!value=tfoot
!!!
<!DOCTYPE HTML><table><caption>$$</col>
!value=col
!!!
!context=caption
$$</caption>
!value=caption
!!!
!context=caption
$$</table>
!value=table
!!!
<!DOCTYPE HTML><table><tr><td>$$</col>
!value=col
!!!
<!DOCTYPE HTML><table><tr><td>$$</body>
!value=body
!!!
<!DOCTYPE HTML><table><tr><td>$$</html>
!value=html
!!!
<!DOCTYPE HTML><table><tr><td>$$</colgroup>
!value=colgroup
!!!
<!DOCTYPE HTML><table><tr><td>$$</caption>
!value=caption
!!!
<!DOCTYPE HTML><table><tr><td>$$</thead>
!value=thead
!!!
!context=tr
<td>$$</tbody>
!value=tbody
!!!
!context=td
$$</table>
!value=table
!!!
<!DOCTYPE HTML><table><tr><td>$$</th>
!value=th
!!!
<!DOCTYPE HTML><table><tr><th>$$</td>
!value=td
!!!
<!DOCTYPE HTML><table><colgroup>$$</col>
!value=col
!!!
!context=colgroup
$$</colgroup>
!value=colgroup
!!!
!context=frameset
$$</frameset>
!value=frameset
!!!
<!DOCTYPE HTML><head>$$</hoge>
!value=hoge
!!!
<!DOCTYPE HTML><head>$$</template>
!value=template
!!!
!noscript
<!DOCTYPE HTML><head><noscript>$$</hoge>
!value=hoge
!!!
<!DOCTYPE HTML><table><tr>$$</body>
!value=body
!!!
!context=tr
$$</table>
!value=table
!!!
!context=tr
$$</tbody>
!value=tbody
!!!
!context=tbody
$$</table>
!value=table
!!!
!context=tr
$$</table>
!value=table
!!!
!context=select
$$</select>
!value=select
!!!
<!DOCTYPE HTML><select>$$</option>
!value=option
!!!
<!DOCTYPE HTML><select>$$</optgroup>
!value=optgroup
!!!
<!DOCTYPE HTML><table><tbody>$$</body>
!value=body
!!!
!context=tbody
$$</thead>
!value=thead
!!!
<!DOCTYPE HTML><table>$$</body>
!value=body
!!!
!context=table
$$</table>
!value=table
!!!
<!DOCTYPE HTML><template>$$</fpopo>
!value=fpopo
!!!
!context=template
<link>$$</template>
!value=template
!!!XML
<p>$$</pq></p>
!value=pq

* not closed before ancestor end tag

name=in-body-end-a3bbcd5f4hhlmmnopssu-2
name=in-body-end-applet-marquee-object-2
name=in-body-end-body-2
name=in-body-end-dd-dt-2
name=in-body-end-form-2
name=in-body-end-form-4
name=in-body-end-h6-2
name=in-body-end-html-2
name=in-body-end-else
name=in-body-end-li-2
name=in-caption-end-caption-2
name=in-caption-end-table-2
name=in-cell-end-td-th-2
name=in-foreign-content-end-else
name=in-foreign-content-end-script
name=in-head-end-template-2
name=-steps-close-a-p-element
name=-steps-close-the-cell
name=in-element-end-else-2
text=current node.local_name
value=tag_name

@en
The <code><var>{text}</var></code> element is not closed before the
<code>&lt;/<var>{value}</var>></code> tag
@ja
この終了タグより前で <code><var>{text}</var></code> 要素が閉じられていません

@en
    <p>Most of elements must be explicitly closed by its end tag, but
    there is no such a tag.  There <i>is</i> an end tag for ancestor
    element, or one of <code>h<var>n</var></code> elements which is
    different from the currently opened element.  The end tag closes
    all descendant or mismatched <code>h<var>n</var></code>
    element.</p>

    <p>Ensure all elements whose end tag can't be omitted have end
    tags, their start and end tag names are matching, and they are not
    misnested.</p>

@ja
    <p>ほとんどの要素は終了タグで明示的に閉じなければいけませんが、
    終了タグがありませんでした。しかし祖先の要素の終了タグか、
    現在開いている要素とは違う <code>h<var>n</var></code> 要素の終了タグはありました。
    それによって子孫や間違った <code>h<var>n</var></code> 
    要素が閉じられます。</p>

    <p>終了タグを省略できない要素がすべて終了タグを持つこと、
    開始タグと終了タグのタグ名が一致していること、
    タグの入れ子関係が正しいことを確認してください。</p>

!!!
<!DOCTYPE HTML><div><span>$$</div>
!text=span
!value=div
!!!
<!DOCTYPE HTML><marquee><span>$$</marquee>
!text=span
!value=marquee
!!!
<!DOCTYPE HTML><body><div>$$</body>
!text=div
!value=body
!!!
<!DOCTYPE HTML><body><dt><span>$$</dt>
!text=span
!value=dt
!!!
<!DOCTYPE HTML><body><h3><span>$$</h3>
!text=span
!value=h3
!!!
<!DOCTYPE HTML><body><form><span>$$</form>
!text=span
!value=form
!!!
<!DOCTYPE HTML><body><template><form><span>$$</form></template>
!text=span
!value=form
!!!
<!DOCTYPE HTML><div>$$</html>
!text=div
!value=html
!!!
<!DOCTYPE HTML><body><hoge><span>$$</hoge>
!text=span
!value=hoge
!!!
<!DOCTYPE HTML><li><span>$$</li>
!text=span
!value=li
!!!
<!DOCTYPE HTML><table><caption><span>$$</caption>
!text=span
!value=caption
!!!
<!DOCTYPE HTML><table><caption><span>$$</table>
!text=span
!value=table
!!!
<!DOCTYPE HTML><table><tr><td><span>$$</td>
!text=span
!value=td
!!!
<!DOCTYPE HTML><svg><g><h>$$</g>
!text=h
!value=g
!!!
<!DOCTYPE HTML><svg><script><h>$$</script>
!text=h
!value=script
!!!
<!DOCTYPE HTML><template><meta><hoge>$$</template>
!value=template
!text=hoge
!!!
<!DOCTYPE HTML><p><span>$$<p>
!value=p
!text=span
!!!
<!DOCTYPE HTML><table><tr><td><span>$$<td>
!value=td
!text=span
!!!XML
<p><q>$$</p>
!value=p
!text=q

* element not closed before implied ancestor end tag

name=in-body-start-dd-dt
name=in-body-start-dd-dt-2
name=in-body-start-li
name=in-body-start-rp-rt
name=in-caption-start-c3t6-2
name=in-foreign-content-start-b5ccd4eeh8iillmmnopprs7ttuuv
name=in-foreign-content-start-font
name=in-select-start-input-keygen-textarea
text=current node.local_name
value=tag_name

@en
The <code>&lt;<var>{text}</var>></code> element is not closed before
a start tag <code><var>{value}</var></code>
@ja
<code><var>{value}</var></code> 開始タグの前で
<code>&lt;<var>{text}</var>></code> 要素が閉じられていません

@en

<p>Some start tag implies an end tag of another element, if necessary.
For example, a <code>&lt;li></code> start tag closes any opening
<code>li</code> element.  Any descendant of that element must be
closed before the start tag (except for a few elements whose end tag
can be omitted, e.g. <code>p</code>).

@ja

<p>一部の開始タグは他の要素を閉じます。例えば
<code>&lt;li></code> 開始タグの時点で開いている
<code>li</code> 要素は閉じられます。
その子孫は (<code>p</code> 要素など一部例外を除き)
開始タグの前に閉じておかなければいけません。

!!!
<!DOCTYPE HTML><dt><span>$$<dt>
!text=span
!value=dt
!!!
<!DOCTYPE HTML><dt><span>$$<dd>
!text=span
!value=dd
!!!
<!DOCTYPE HTML><li><span>$$<li>
!text=span
!value=li
!!!
<!DOCTYPE HTML><ruby><rp><span>$$<rt>
!text=span
!value=rt
!!!
<!DOCTYPE HTML><ruby><rt><span>$$<rp>
!text=span
!value=rp
!!!
<!DOCTYPE HTML><table><caption><span>$$<caption>
!text=span
!value=caption
!!!
<!DOCTYPE HTML><table><caption><span>$$<tr>
!text=span
!value=tr
!!!
<!DOCTYPE HTML><svg>$$<p>
!text=svg
!value=p
!!!
<!DOCTYPE HTML><svg>$$<font face>
!text=svg
!value=font
!!!
<!DOCTYPE HTML><select>$$<input>
!text=select
!value=input

* AAA:in afe but not in open elements

value=tag_name
layer=tree-construction
module=Web::HTML::Parser::tree_constructor

@en
The end tag closes a misnested element
@ja
この終了タグは入れ子関係がおかしくなった要素を閉じています

@en

<p>There are some misnested elements; an element opened before another
element is closed before the other element.  The end tag closes the
other element, but it should have been appeared before the first
element's end tag.

@ja

<p>入れ子関係がおかしな要素があるようです。
ある要素が別の要素より先に開かれましたが、
その別の要素より先に最初の要素が閉じられています。
この終了タグはその別の要素のものですが、最初の要素の終了タグより前に置かなければいけません。

!!!
<!DOCTYPE HTML><span><b></span>$$</b>
!value=b

* AAA:formatting element not current

value=tag_name
layer=tree-construction
module=Web::HTML::Parser::tree_constructor

@en
The end tag closes a misnested element
@ja
この終了タグは入れ子関係がおかしくなった要素を閉じています

@en

<p>There are some misnested elements; an element opened before another
element is closed before the other element.  The end tag closes the
first element, but it should have been appeared after the other
element's end tag.

@ja

<p>入れ子関係がおかしな要素があるようです。
ある要素が別の要素より先に開かれましたが、
その別の要素より先に最初の要素が閉じられています。
この終了タグはその最初の要素のものですが、
後の要素の終了タグより後に置かなければいけません。

!!!
<!DOCTYPE HTML><b><div>$$</b>
!value=b

* AAA:formatting element not in scope

value=tag_name
layer=tree-construction
module=Web::HTML::Parser::tree_constructor

@en
The end tag cannot close the element as its descendant is not closed
@ja
子孫が閉じられていないので、この終了タグは要素を閉じられません

@en

<p>There must be an explicit end tag for the elements, except for a
few HTML elements.  The end tag is ignored as there is some element
descendant that cannot be closed by the end tag.

@ja

<p>一部の HTML 要素を除き、要素の終了タグは明記しないといけません。
この終了タグでは閉じられない子孫要素があるため、無視されます。

!!!
<!DOCTYPE HTML><b><table>b$$</b>a
!value=b

* no DOCTYPE name

name=before-doctype-name-003e
name=before-doctype-name-005b

@en
There is no document type name
@ja
文書型名がありません

@en

<p>The DOCTYPE must be <code>&lt;!DOCTYPE html></code> (in HTML) or
must contain the root element name (in XML).

@ja

<p>DOCTYPE は <code>&lt;!DOCTYPE html></code> とする (HTML)
か、根要素の名前を含める (XML) かしなければいけません。

* bogus DOCTYPE

name=after-doctype-name-else
name=after-doctype-public-identifier-else
name=after-doctype-public-keyword-else
name=after-doctype-system-identifier-else
name=after-doctype-system-keyword-else
name=before-doctype-public-identifier-else
name=before-doctype-system-identifier-else
name=between-doctype-public-and-system-identifiers-else
name=doctype-else

@en
There is a bogus string in the DOCTYPE
@ja
DOCTYPE 内におかしな文字列があります

@en

<p>There is a bogus string within the DOCTYPE.  The DOCTYPE must be
<code>&lt;!DOCTYPE&#x20;HTML></code>.  No other string is required.

@ja
DOCTYPE 内におかしな文字列があります。 DOCTYPE は
<code>&lt;!DOCTYPE&#x20;HTML></code> だけでよく、
それ以外の文字列を書いてはいけません。

!!!
<!DOCTYPE html $$!>

!!!
<!DOCTYPE html PUBLIC$$!>

!!!
<!DOCTYPE html public "a"$$!>

!!!
<!DOCTYPE html SYSTEM$$?>

!!!
<!DOCTYPE html system ""$$@p>

!!!
<!DOCTYPE html PUBLIC "abc" "http://www.w3.org/TR/html4/strict.dtd"$$?>
!!!
<!DOCTYPE html PUBLIC $$hoge>
!!!
<!DOCTYPE html SYSTEM $$hoge>
!!!
<!DOCTYPE html PUBLIC "hoge"$$hoge>
!!!
<!DOCTYPE html PUBLIC "hoge" $$hoge>
!!!
<!DOCTYPE$$!>

* no space before literal

name=after-doctype-public-keyword-0022
name=after-doctype-public-keyword-0027
name=after-doctype-public-identifier-0022
name=after-doctype-public-identifier-0027
name=after-doctype-system-keyword-0022
name=after-doctype-system-keyword-0027

@en
There is no space before a quoted string
@ja
括られた文字列の前に空白がありません

@en
<p>There must be one or more white space characters between the
keyword <code>PUBLIC</code> or <code>SYSTEM</code> and the quoted
string, and between two quoted strings.

@ja
<p>マーク付け宣言の引数であるキーワードや引用符で括られた文字列、群の間は1文字以上の空
白が必要です。

!!!
<!DOCTYPE html PUBLIC$$"abc">

!!!
<!DOCTYPE html SYSTEM$$"">

!!!
<!DOCTYPE html PUBLIC ""$$"">

!!!
<!DOCTYPE html PUBLIC$$'abc'>

!!!
<!DOCTYPE html SYSTEM$$''>

!!!
<!DOCTYPE html PUBLIC "abc"$$'http'>

* no space between params

name=after-allowed-token-list-else
name=attlist-attribute-type-0023
name=attlist-attribute-default-0022
name=attlist-attribute-default-0027
name=after-attlist-default-value-else
name=attlist-attribute-name-0028
name=attlist-attribute-type-0022
name=attlist-attribute-type-0027
name=attlist-attribute-type-0028
name=attlist-else
name=after-notation-public-identifier-0027
name=after-notation-public-identifier-0022
name=notation-else
name=element-else
name=after-entity-system-identifier-else
name=after-entity-public-identifier-0022
name=after-entity-public-identifier-0027
name=parameter-entity-declaration-or-reference-003e
name=after-ndata-keyword-else
name=parameter-entity-declaration-or-reference-003d
name=parameter-entity-declaration-or-reference-003c
name=parameter-entity-declaration-or-reference-0022
name=parameter-entity-declaration-or-reference-0026
name=parameter-entity-declaration-or-reference-0027
name=parameter-entity-declaration-or-reference-0060
name=parameter-entity-declaration-or-reference-0025
name=parameter-entity-declaration-or-reference-ws
name=after-entity-public-keyword-0022
name=after-entity-public-keyword-0027
name=after-entity-system-keyword-0022
name=after-entity-system-keyword-0027
name=entity-else
name=entity-name-0022
name=entity-name-0027
name=element-name-0028
name=after-notation-public-keyword-0022
name=after-notation-public-keyword-0027
name=after-notation-system-keyword-0022
name=after-notation-system-keyword-0027
name=after-notation-public-keyword-else
name=after-notation-system-keyword-else

@en
There is no space between markup declaration parameters
@ja
マーク付け宣言の引数の間に空白がありません

@en
<p>There must be one or more white space characters between markup
declaration parameters such as keywords, literals, and groups.

@ja
<p>マーク付け宣言の引数であるキーワードや引用符で括られた文字列、群の間は1文字以上の空白が必要です。

!!!XML
<!DOCTYPE x [<!ATTLIST a b (c)$$"d">]><x/>
!!!XML
<!DOCTYPE x [<!ENTITY%$$=>]><x/>
!!!XML
<!DOCTYPE x [<!ENTITY%$$<>]><x/>

* no DOCTYPE literal

name=after-doctype-public-keyword-003e
name=after-doctype-system-keyword-003e
name=before-doctype-public-identifier-003e
name=before-doctype-system-identifier-003e
name=after-notation-public-keyword-003e
name=after-notation-system-keyword-003e
name=before-notation-public-identifier-003e
name=before-notation-system-identifier-003e
name=before-notation-public-identifier-else
name=before-entity-public-identifier-003e
name=before-entity-system-identifier-003e
name=before-entity-system-identifier-else
name=after-entity-public-keyword-003e
name=between-entity-public-and-system-identifiers-else
name=after-entity-public-identifier-else
name=after-entity-system-keyword-003e
name=before-entity-public-identifier-else
name=before-doctype-public-identifier-005b
name=after-doctype-public-identifier-005b
name=before-doctype-system-identifier-005b
name=after-doctype-public-keyword-005b
name=after-doctype-system-keyword-005b
name=between-doctype-public-and-system-identifiers-005b
name=after-doctype-public-identifier-003e
name=between-doctype-public-and-system-identifiers-003e
name=between-entity-public-and-system-identifiers-003e
name=after-entity-public-identifier-003e
name=after-entity-public-keyword-else
name=after-entity-system-keyword-else

@en
There is no quoted string
@ja
引用符で括られた文字列がありません

@en

<p>There must be a quoted string after the <code>PUBLIC</code> or
<code>SYSTEM</code> keyword, containing the public or system
identifier.

@ja

<p>キーワード <code>PUBLIC</code> や <code>SYSTEM</code>
の後には、公開識別子かシステム識別子を引用符で括った文字列がなければいけません。

!!!
<!DOCTYPE html PUBLIC$$>

!!!
<!DOCTYPE html PUBLIC $$>

!!!
<!DOCTYPE html system$$>

!!!
<!DOCTYPE html system  $$>

* parser:literal not closed

name=doctype-public-identifier-double-quoted-003e
name=doctype-public-identifier-single-quoted-003e
name=doctype-system-identifier-double-quoted-003e
name=doctype-system-identifier-single-quoted-003e
name=entity-public-identifier-double-quoted-003e
name=entity-public-identifier-single-quoted-003e
name=notation-public-identifier-double-quoted-003e
name=notation-public-identifier-single-quoted-003e

@en
The quoted string is not terminated before the declaration
is closed
@ja
宣言が閉じられる前に閉じ引用符がありません

@en

<p>The public or system identifier must be terminated by an
appropriate quotation mark before the declaration in which the
identifier appears is closed by <code>></code>.

<p>The public or system identifier cannot contain a <code>></code>
character.

@ja

<p>公開識別子やシステム識別子は、宣言が <code>></code>
で閉じられる前に適切な引用符で閉じなければいけません。

<p>公開識別子やシステム識別子に <code>></code>
を含めることはできません。

!!!
<!DOCTYPE HTML PUBLIC "$$>">
!!!
<!DOCTYPE HTML SYSTEM "$$>">
!!!
<!DOCTYPE HTML PUBLIC "" "$$>">
!!!
<!DOCTYPE HTML PUBLIC '$$>'>
!!!
<!DOCTYPE HTML SYSTEM '$$>'>
!!!
<!DOCTYPE HTML PUBLIC "" '$$>'>

* string after PUBLIC literal

name=between-notation-public-and-system-identifiers-else
name=after-notation-public-identifier-else

@en
There is an unknown parameter after the public identifier
@ja
公開識別子の後に未知の引数があります

@en
<p>The public identifier cannot be followed by parameters other than
the system identifier in a <code>NOTATION</code> declaration.
@ja
<p><code>NOTATION</code> 宣言では公開識別子の後にシステム識別子以外の引数は指定できません。

!!!XML
<!DOCTYPE a[<!NOTATION a PUBLIC "b"$$a]><a/>

* string after SYSTEM literal

name=after-notation-system-identifier-else
name=before-ndata-keyword-else

@en
There is an unknown parameter after the system identifier
@ja
システム識別子の後に未知の引数があります

@en
<p>The system identifier cannot be followed by another parameter
in a <code>NOTATION</code> declaration.
@ja
<p><code>NOTATION</code> 宣言ではシステム識別子の後に他の引数は指定できません。

!!!XML
<!DOCTYPE a[<!NOTATION a SYSTEM "b"$$a]><a/>

* no DOCTYPE

name=initial-else

@en
There is no DOCTYPE
@ja
DOCTYPE がありません

@en

<p>The document does not have the DOCTYPE.  An HTML document must
begin with the DOCTYPE (<code>&lt;!DOCTYPE&#x20;HTML></code>).

<p>Before the DOCTYPE, only white space characters and comments are
allowed.

@ja

<p>この文書は DOCTYPE から始まっていません。HTML 文書は DOCTYPE
(<code>&lt;!DOCTYPE&#x20;HTML></code>) から始まらなければいけません。

!!!
$$hoge

!!!
<!---->$$hoge

* doctype:bad context

name=after-body-doctype
name=after-frameset-doctype
name=after-head-doctype
name=before-head-doctype
name=before-html-doctype
name=in-body-doctype
name=in-column-group-doctype
name=in-foreign-content-doctype
name=in-frameset-doctype
name=in-head-doctype
name=in-head-noscript-doctype
name=in-select-doctype
name=in-table-doctype
name=before-root-element-doctype
name=in-element-doctype
name=after-root-element-doctype

@en
The DOCTYPE is not at the beginning of the document
@ja
DOCTYPE が文書の先頭ではありません

@en

<p>There must not be anything other than white space characters and
comments before the DOCTYPE.  It cannot appear after another DOCTYPE
or within an element.

@ja

<p>DOCTYPE の前に置けるのは空白と注釈だけです。DOCTYPE が2つあったり、
要素の中にあったりしてはいけません。

!!!
<!DOCTYPE html>$$<!DOCTYPE HTML>
!!!
<!DOCTYPE html><html>$$<!DOCTYPE HTML>
!!!
<!DOCTYPE HTML></body>$$<!DOCTYPE HTML>
!!!
<!DOCTYPE HTML><frameset></frameset>$$<!DOCTYPE HTML>
!!!
<!DOCTYPE HTML></head>$$<!DOCTYPE HTML>
!!!
<!DOCTYPE HTML><body>$$<!DOCTYPE HTML>
!!!
<!DOCTYPE HTML><table><colgroup>$$<!DOCTYPE HTML>
!!!
<!DOCTYPE HTML><svg>$$<!DOCTYPE HTML>
!!!
<!DOCTYPE HTML><frameset>$$<!DOCTYPE HTML>
!!!
<!DOCTYPE HTML><head>$$<!DOCTYPE HTML>
!!!
!noscript
<!DOCTYPE HTML><head><noscript>$$<!DOCTYPE HTML>
!!!
<!DOCTYPE HTML><select>$$<!DOCTYPE HTML>
!!!
<!DOCTYPE HTML><table>$$<!DOCTYPE HTML>

* bad DOCTYPE name

layer=tree-construction
module=Web::HTML::Parser::tokenizer

@en
The DOCTYPE name <code><var>{value}</var></code> is not the
root element name <code><var>{text}</var></code>
@ja
DOCTYPE の名前 <code><var>{value}</var></code> が根要素の名前
<code><var>{text}</var></code> と違います

@en

<p>The name in the DOCTYPE must be equal to the qualified name of the
root element.  In HTML document, it must be <code>html</code>.

@ja

<p>DOCTYPE の名前は根要素の修飾名と同じでなければいけません。
HTML 文書では <code>html</code> でなければいけません。

!!!
$$<!DOCTYPE foo>
!text=html
!value=foo
!!!
$$<!DOCTYPE html4>
!text=html
!value=html4

* obsolete DOCTYPE

name=initial-doctype
layer=tree-construction
module=Web::HTML::Parser::tokenizer

@en
The DOCTYPE has public and/or system identifiers
@ja
DOCTYPE に公開識別子やシステム識別子があります

@en

<p>The DOCTYPE has public and/or system identifiers, which are
historical constructs that are no longer useful.  You can simply write
<code>&lt;!DOCTYPE&#x20;HTML></code>.

@ja

<p>DOCTYPE に公開識別子やシステム識別子がありますが、
これらは歴史的なもので、もはや意味がありません。単純に
<code>&lt;!DOCTYPE&#x20;HTML></code> と書くだけで十分です。

!!!
$$<!DOCTYPE HTML PUBLIC "hoge">
!!!
$$<!DOCTYPE HTML PUBLIC "hoge" "fuga">
!!!
$$<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "fa">
!!!
$$<!DOCTYPE HTML SYSTEM "hoge">
!!!
$$<!DOCTYPE HTML SYSTEM "http://www.w3.org/TR/html4/strict.dtd">
!!!
$$<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN">
!!!
$$<!DOCTYPE HTML system "">

* obsolete permitted DOCTYPE

default_level=s
layer=tree-construction
module=Web::HTML::Parser::tokenizer

@en
The DOCTYPE has public and/or system identifiers
@ja
DOCTYPE に公開識別子やシステム識別子があります

@en

<p>The DOCTYPE has one of public identifiers of HTML4/XHTML1 age,
which is no longer necessary.  You can simply write
<code>&lt;!DOCTYPE&#x20;HTML></code>.

@ja

<p>DOCTYPE に HTML4/XHTML1 時代の公開識別子やシステム識別子がありますが、
これはもう必要ありません。単純に
<code>&lt;!DOCTYPE&#x20;HTML></code> と書くだけで十分です。

!!!
$$<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN">

* legacy DOCTYPE

default_level=s
layer=tree-construction
module=Web::HTML::Parser::tokenizer

@en
The DOCTYPE has a system identifier
@ja
DOCTYPE にシステム識別子があります

@en

<p>The DOCTYPE has a legacy DOCTYPE string system identifier
(<code>about:legacy-compat</code>), which is hardly necessary.  You
can simply write <code>&lt;!DOCTYPE&#x20;HTML></code>.

@ja

<p>DOCTYPE に遺物 DOCTYPE 文字列のシステム識別子がありますが、
これは滅多に必要ありません。単純に
<code>&lt;!DOCTYPE&#x20;HTML></code> と書くだけで十分です。

!!!
$$<!DOCTYPE HTML SYSTEM "about:legacy-compat">

* xml:pubid:bad char

layer=entity
module=Web::XML::Parser::checker

@en
The public identifier contains an invalid character
@ja
公開識別子に認められない文字が含まれています

@en
<p>A public identifier can contain only limited set of characters.
@ja
<p>公開識別子には限られた文字しか含めることができません。

* xml:pubid:not normalized

layer=entity
module=Web::XML::Parser::checker

@en
The public identifier contains non-normalized spaces
@ja
公開識別子に正規化されていない空白が含まれています

@en
<p>Leading, trailing, or consecutive spaces, as well as newline
characters, are expected to be normalized by parser.  However, this is not
an interoperable behavior.
@ja
<p>先頭、末尾、連続の空白や改行は構文解析器により正規化されることが期待されています。
しかしこれは相互運用性が高くありません。

* after-after-body-else

name=after-after-body-else
name=after-after-frameset-else

@en
There is a text or tag after the <code>&lt;/html></code> end tag
@ja
<code>&lt;/html></code> 終了タグの後にテキストやタグがあります

@en

<p>Nothing other than white space characters and comments are allowed
after the end tag of the <code>html</code> element.

@ja

<p><code>html</code> 要素の終了タグの後には、空白と注釈以外は何も書けません。

!!!
<!DOCTYPE html></html>$$<p>

* after-body-else

name=after-body-else
name=after-frameset-else

@en
There is a text or tag after the end tag of the body element
@ja
文書 <code>body</code> 要素の終了タグの後にテキストやタグがあります

@en

<p>Nothing other than the end tag of the <code>html</code> element,
white space characters, and comments are allowed after the end tag of
the <code>body</code> element or the outermost <code>frameset</code>
element.

@ja

<p><code>body</code> 要素や最外 <code>frameset</code> 要素の終了タグの後には、
<code>html</code> 要素の終了タグと空白、注釈以外は何も書けません。

* bad attribute name

name=attribute-name-0022
name=attribute-name-0027
name=before-attribute-name-0022
name=before-attribute-name-0027

@en
The attribute name contains a <code>"</code> or <code>'</code>
@ja
属性名に <code>"</code> や <code>'</code> が含まれています

@en

<p>An attribute name cannot contain a <code>"</code> or <code>'</code>
character, as it is used as a quotation mark of an attribute value.

@ja

<p><code>"</code> や <code>'</code> は属性値の引用符として使われるので、
属性名に含めることはできません。

!!!
<!DOCTYPE html><p foo$$"aa>
!!!
<!DOCTYPE html><p foo$$'aa>

* parser:no attr name

name=before-attribute-name-003d

@en
The attribute name is missing before <code>=</code>
@ja
<code>=</code> の前に属性名がありません

@en

<p>An attribute in a start tag must have a name.  The attribute name
cannot contain a <code>=</code> character, as it is used to separate
attribute name and value.

@ja

<p>開始タグの属性には名前が必要です。属性名には <code>=</code>
を含められません。 <code>=</code> は属性名と属性値の区切りとして使われます。

!!!
<!DOCTYPE html><p $$=foo>

* no attr value

name=attribute-name-002f
name=attribute-name-003e
name=after-attribute-name-else
name=after-attribute-name-upper
name=after-attribute-name-002f
name=after-attribute-name-003e

@en
There is no attribute value
@ja
属性値がありません

@en
<p>There must be attribute value in XML.
@ja
<p>XML では属性値は必須です。

!!!XML
<p foo$$/>
!!!XML
<p foo$$></p>
!!!XML
<p foo $$x=""/>
!!!XML
<p foo $$/>
!!!XML
<p foo $$>

* bad attribute value

name=attribute-value-unquoted-0022
name=attribute-value-unquoted-0027
name=attribute-value-unquoted-003d
name=attribute-value-unquoted-0060
name=before-attribute-value-003d
name=before-attribute-value-003e
name=before-attribute-value-0060

@en
An unquoted attribute value contains <code>"</code>, <code>'</code>,
<code>`</code>, or <code>=</code>

@ja
引用符で括られていない属性値に <code>"</code>, <code>'</code>,
<code>`</code>, <code>=</code> が含まれています

@en

<p>In an unquoted attribute value, characters <code>"</code>
(<code>U+0022</code> <code class="charname">QUOTATION MARK</code>),
<code>'</code> (<code>U+0026</code> <code
class="charname">APOSTROPHE</code>), and <code>=</code>
(<code>U+003D</code> <code class="charname">EQUAL SIGN</code>) are not
allowed.  To contain these characters in an attribute value, the
entire attribute value must be quoted by <code>"</code> or
<code>'</code>.

<p>Note that an unquoted attribute value cannot be the empty string.
For example, <code class="html bad
example">&lt;foo&#x20;<mark>bar=&#x20;baz=</mark>></code> is
interpreted to have a <code>bar</code> attribute whose value is
<code>bar=</code> (with a parse error as the <code>=</code> character
cannot be used in an unquoted attribute value).  To represent empty
attribute value, omit the <code>=</code> character, or explicitly
specify an empty quoted string: <code class="html bad
example">&lt;foo&#x20;bar&#x20;baz=<mark>""</mark>></code>.

@ja

<p>引用符で括られていない属性値では <code>"</code>
(<code>U+0022</code> <code class="charname">QUOTATION MARK</code>),
<code>'</code> (<code>U+0026</code> <code
class="charname">APOSTROPHE</code>), <code>=</code>
(<code>U+003D</code> <code class="charname">EQUAL SIGN</code>)
は使えません。属性値にこれらを含めたいときは、
全体を <code>"</code> や <code>'</code> で括らなければいけません。

<p>なお、引用符で括られていない属性値は空文字列にできません。例えば、
<code class="html bad
example">&lt;foo&#x20;<mark>bar=&#x20;baz=</mark>></code>
は <code>bar</code> 属性の値が <code>bar=</code> (で、 <code>=</code>
を引用符で括られていない属性値では使えないため構文解析エラー)
と解釈されます。属性値が空であることを表すには、 <code class="html bad
example">&lt;foo&#x20;bar&#x20;baz=<mark>""</mark>></code>
のように <code>=</code> を省略するか、引用符を明示するかしないといけません。

!!!
<!DOCTYPE HTML><p foo=bar$$">
!!!
<!DOCTYPE HTML><p foo=bar$$'>
!!!
<!DOCTYPE HTML><p foo=bar$$`>
!!!
<!DOCTYPE HTML><p foo=bar$$=>
!!!
<!DOCTYPE HTML><p foo=$$`>
!!!
<!DOCTYPE HTML><p foo=$$=>
!!!
<!DOCTYPE HTML><p foo= bar$$=>

* lt in attr value

name=attribute-value-double-quoted-003c
name=attribute-value-single-quoted-003c
name=default-attribute-value-double-quoted-003c
name=default-attribute-value-single-quoted-003c

@en
There is <code>&lt;</code> in an attribute value
@ja
属性値に <code>&lt;</code> があります

@en
<p>The <code>&lt;</code> character is not allowed in an attribute
value.  It must be escaped as <code>&amp;lt;</code>.
@ja
<p>属性値に <code>&lt;</code> を含めることはできません。 <code>&amp;lt;</code>
とエスケープしなければいけません。

!!!XML
<p x="a$$<b"/>
!!!XML
<p x='a$$<b'/>

* attr:no =

name=after-attribute-name-0022
name=after-attribute-name-0027

@en
No <code>=</code> between attribute name and value

@ja
属性名と属性値の間に <code>=</code> がありません

@en

<p>There must be a <code>=</code> character between an attribute name
and an attribute value.  Characters <code>"</code> and <code>'</code>
cannot be used as part of an attribute name or an unquoted attribute
value.

@ja

<p>属性名と属性値の間には文字 <code>=</code> 
が必要です。また、文字 <code>"</code> や文字 <code>'</code>
を属性名や括られていない属性値に使うことはできません。

!!!
<!DOCTYPE html><foo bar $$"></foo>

!!!
<!DOCTYPE html><foo bar $$'></foo>

* unquoted attr value

name=before-attribute-value-else
name=before-attribute-value-0026
name=after-attlist-attribute-type-else
name=after-after-allowed-token-list-else

@en
The attribute value is not quoted
@ja
属性値が引用符で括られていません

@en
<p>In XML, the attribute value must be quoted by <code>"</code>
or <code>'</code>.
@ja
<p>XML では属性値は <code>"</code> か <code>'</code> で括らなければいけません。

!!!XML
<a b=$$c/>
!!!XML
<a b=$$&copy;/>
!!!XML
<!DOCTYPE a[<!ATTLIST a b CDATA $$foo>]><a/>

* tag not closed

name=after-attribute-name-003c
name=attribute-name-003c
name=before-attribute-name-003c
name=attribute-value-unquoted-003c
name=before-attribute-value-003c

@en
Tag is not closed
@ja
タグが閉じられていません

@en

<p>A start or end tag must be closed by a <code>></code> character
before following text or another tag.

<p>A bare <code>&lt;</code> character cannot be used in start or end
tags (except within quoted attribute values).

@ja

<p>開始タグや終了タグは、次のテキストや他のタグの前に文字
<code>></code> で閉じなければいけません。

<p>文字 <code>&lt;</code> をそのまま開始タグや終了タグの中
(括られた属性値以外) で使ってはいけません。

!!!
<!DOCTYPE html><p foo $$<bar>
!!!
<!DOCTYPE html><p foo$$<bar>
!!!
<!DOCTYPE html><p $$<bar>
!!!
<!DOCTYPE HTML><p foo=$$<bar>
!!!
<!DOCTYPE HTML><p foo=a$$<bar>

* nestc has no net

name=self-closing-start-tag-else

@en
The tag is not closed by <code>></code> after <code>/</code>
@ja
タグが <code>/</code> の後 <code>></code> で閉じられていません

@en

<p>When an element is self-closing, the start tag must be closed by
<code>/></code>.  The <code>/</code> character must be immediately
followed by a <code>></code> character.

@ja

<p>要素をその場で閉じて内容を空とするときは、
開始タグは <code>/></code> で終えなければいけません。
<code>/</code> の直後に <code>></code> が必要です。

!!!
<!DOCTYPE HTML><p /$$ >
!!!
<!DOCTYPE HTML><p/$$hoge>

* nestc

name=-start-tag-self-closing-flag
text=tag_name
layer=tree-construction
module=Web::HTML::Parser::tokenizer
module=Web::XML::Parser::tokenizer

@en
The self-closing syntax (<code>/></code>) is used
@ja
その場で要素を閉じる構文 (<code>/></code>) が使われています

@en

<p>The self-closing start tag syntax (<code>/></code>) has effect only
for SVG and MathML elements.  For HTML void elements such as
<code>br</code> and <code>img</code>, it is allowed but has no effect.
For the other HTML elements, <code>/</code> is simply ignored and is
not allowed.

<p>The self-closing tag syntax is not allowed in an end tag.

@ja

<p>その場で要素を閉じるタグの構文 (<code>/></code>) は SVG や
MathML の要素でのみ意味があります。 HTML の void 要素
(<code>br</code> や <code>img</code> など) でも認められていますが、
意味はありません。それ以外の HTML 要素では
<code>/</code> は無視されるだけなので、認められていません。

<p>その場で閉じる構文は、終了タグでは使えません。

!!!
<!DOCTYPE HTML>$$<p/>
!text=p
!!!
<!DOCTYPE HTML>$$<img/>
!text=img
!level=w
!!!
<!DOCTYPE HTML><p>$$</p/>
!text=p

* xml:empty element tag:non-EMPTY

layer=dtd
module=Web::XML::Parser::tokenizer

@en
A non-<code>EMPTY</code> element is using the empty-element tag syntax
@ja
<code>EMPTY</code> でない要素が空要素タグ構文を使っています

@en
<p>An element that is not declared with the <code>EMPTY</code> keyword
should not use the empty-element tag syntax (i.e. <code>/></code>).
@ja
<p><code>EMPTY</code> キーワード無しで宣言された要素は空要素タグ構文
(<code>/></code>) を使うべきではありません。

* xml:start tag:EMPTY

layer=dtd
module=Web::XML::Parser::tokenizer

@en
An <code>EMPTY</code> element does not use the empty-element tag syntax
@ja
<code>EMPTY</code> 要素が空要素タグ構文を使っていません

@en
<p>An element that is declared with the <code>EMPTY</code> keyword
should use the empty-element tag syntax (i.e. <code>/></code>).
@ja
<p><code>EMPTY</code> キーワードが指定された要素は空要素タグ構文
(<code>/></code>) を使うべきです。

* no space before attr name

name=after-attribute-value-quoted-else
module=Web::HTML::Parser::tokenizer
module=Web::XML::Parser::tokenizer

@en
There is no space before an attribute
属性の前に空白がありません

@en

    <p>There must be one or more white space characters between
    attributes and before the first attribute.</p>

    <p>Character <code>U+000C</code> cannot be used as a space before
    an attribute in an HTML document.</p>

    <p>Characters <code>U+0085</code> and <code>U+202B</code> cannot
    be used as a space before an attribute in an HTML or XML 1.0
    document.</p>

@ja

    <p>属性の間や最初の属性の前には空白文字が1つ以上必要です。</p>

    <p>文字 <code>U+000C</code> を XML 文書の属性の前の空白として使うことはできません。</p>

    <p>文字 <code>U+0085</code> や文字 <code>U+202B</code>
    を HTML や XML 1.0 の文書の属性の前の空白として使うことはできません。</p>

!!!
<!DOCTYPE html><p foo=""$$bar="">
!!!XML
<?xml version="1.0"$$encoding="utf-8"?><a/>

* end tag attribute

layer=tokenizer
module=Web::HTML::Parser::tokenizer
module=Web::XML::Parser::tokenizer

@en
An attribute is specified for an end tag
@ja
終了タグに属性が指定されています

@en
<p>An end tag cannot have attributes.
@ja
<p>終了タグに属性は指定できません。

!!!
<!DOCTYPE HTML><div class=foo>aa$$</div class=foo>

* duplicate attribute

layer=tokenizer
module=Web::HTML::Parser::tokenizer
module=Web::XML::Parser::tokenizer
module=Temma::Parser

@en
There are multiple <code><var>{text}</var></code> attributes
@ja
属性 <code><var>{text}</var></code> が複数あります

@en
    <p>There are more than one attributes with the same name in a tag.</p>

    <dl class="switch">

    <dt>HTML <code>img</code> element, the <code>motion</code>
    attribute</dt>

    <dd>This attribute is obsolete.  Use animation GIF instead.</dd>

    </dl>

@ja

    <p>同じ名前の属性を複数個同じタグに指定することはできません。</p>

    <dl class="switch">

    <dt>HTML <code>img</code> 要素 <code>motion</code> 属性</dt>

    <dd>この属性は廃止されました。代わりにアニメーション GIF
    を使ってください。</dd>

    </dl>

!!!
<!DOCTYPE HTML><div class=foo $$class=bar>
!text=class

* empty end tag

name=end-tag-open-003e
module=Whatpm::WebVTT::Parser

@en
There is an empty end tag
@ja
空の終了タグがあります

@en
    <p>In HTML, XML, and WebVTT, empty end tag, i.e. end
    tag with no tag name, is not allowed.</p>
    <p>To represent an end tag, specify the
    tag name explicitly.</p>
    <p>To represent a string with the <code>&lt;</code> character,
    use a character reference: <code>&amp;lt;</code>.</p>

@ja
    <p>HTML、XML、WebVTT では、タグ名の無い空の終了タグは認められていません。</p>
    <p>終了タグを記述する時はタグ名も明示しなければなりません。</p>
    <p>「<code>&lt;</code>」という文字を使いたい時は文字参照
    「<code>&amp;lt;</code>」を使わなければなりません。</p>

!!!
<!DOCTYPE HTML><p></$$>

* bare stago

name=tag-open-else
name=tag-open-003e
name=tag-open-ws
name=doctype-tag-open-else
module=Whatpm::WebVTT::Parser

@en
The <code>&lt;</code> character is not followed by a tag name or a
<code>!</code>, <code>/</code>, or <code>?</code>
@ja
文字 <code>&lt;</code> の次がタグ名でも
<code>!</code>, <code>/</code>, <code>?</code> のいずれでもありません

@en
    <p>A <code>&lt;</code> (<code class="char">U+003C</code> <code
    class="charname">LESS-THAN SIGN</code>) character which is not
    part of any markup appears in the input.

    <p>The <code>&lt;</code> character as a data character must
    be escaped as:
    </p>
    <pre class="html example"><code>&amp;lt;</code></pre>

@ja
    <p>文字 <code>&lt;</code> (<code class="char">U+003C</code> <code
    class="charname">LESS-THAN SIGN</code>) が出現しましたが、
    正しいマーク付けの一部ではありません。</p>

    <p>文字 <code>&lt;</code> をデータ文字として表したい時は、
    エスケープしなければなりません。</p>
    <pre class="html example"><code>&amp;lt;</code></pre>

!!!
<!DOCTYPE HTML><$$"
!!!XML
<p>$$<></p>
!!!XML
<!DOCTYPE a[$$<b>]><a/>

* bare etago

name=end-tag-open-else
name=end-tag-open-ws

@en
The <code>&lt;/</code> is not followed by a tag name
@ja
<code>&lt;/</code> の後にタグ名がありません

@en

<p>There is a <code>&lt;</code> (<code>U+003C</code> <code
class="charname">LESS-THAN SIGN</code>) character immediately followed
by a <code>/</code> (<code>U+005F</code> <code>SOLIDUS</code>)
character, which is not followed by a tag name.

<p>To represent a string <code>&lt;/</code>, the <code>&lt;</code>
character must be escaped as <code>&amp;lt;</code>.

@ja

<p><code>&lt;</code> (<code>U+003C</code> <code
class="charname">LESS-THAN SIGN</code>) の後に
<code>/</code> (<code>U+005F</code> <code>SOLIDUS</code>)
がありますが、その後にタグ名がありません。

<p><code>&lt;/</code> という文字列を表したい時は、 <code>&lt;</code>
を <code>&amp;lt;</code> とエスケープしなければいけません。

!!!
<!DOCTYPE HTML></$$!?>
!!!XML
<!DOCTYPE HTML>$$</ >

* bare pio

name=pi-003f
name=pi-ws

@en
There is no processing instruction target
@ja
処理指令の対象名がありません

@en
<p>After the <code>&lt;?</code> string, there must be a processing instruction's
target name.  To represent a string <code>&lt;?</code>, the <code>&lt;</code>
character must be escaped as <code>&amp;lt;</code>.
@ja
<p><code>&lt;?</code> の後には処理指令の対象名が必要です。文字列
<code>&lt;?</code> を表したい時は <code>&lt;</code> を
<code>&amp;lt;</code> とエスケープしないといけません。

!!!XML
<p>Aa$$<??>
!!!XML
<p>Aa$$<? ?>

* bogus comment

name=markup-declaration-open-else
name=doctype-markup-declaration-open-else

@en
The <code>&lt;!</code> is not followed by <code>--</code>
@ja
<code>&lt;!</code> の後に <code>--</code> がありません

@en
    <p>There is a <code>&lt;</code> (<code>U+003C</code> 
    <code class="charname">LESS-THAN SIGN</code>) character
    followed by a <code>!</code> (<code>U+0021</code>
    <code class="charname">EXCLAMATION MARK</code>) character,
    which is not followed by a <code>--</code> or
    <code>DOCTYPE</code>.

    <dl class="switch">
    <dt>Comments</dt>
      <dd>In HTML document, comments must be introduced by
      <code class="example">&lt;!--</code> (<code>&lt;!</code>
      <em>immediately</em> followed
      by <em>two</em> <code>-</code>s) and must be terminated by
      <code class="example">--></code>.
      Strings <code>&lt;!</code> not followed
      by <code>--</code> and <code>&lt;!-</code> not followed by
      <code>-</code> are not valid open delimiters for comments.</dd>
    <dt>Marked sections, including <code>CDATA</code> sections</dt>
      <dd>Marked sections are not allowed in HTML elements.</dd>
    <dt>Markup declarations</dt>
      <dd>Markup declarations, except for <code>DOCTYPE</code>
      and comment declarations, are not allowed in HTML document.</dd>
    <dt>String <code>&lt;!</code></dt>
      <dd>String <code>&lt;!</code> must be escaped as
      <code class="example">&amp;lt;!</code>.</dd>
    </dl>

@ja

    <p><code>&lt;</code> (<code>U+003C</code> 
    <code class="charname">LESS-THAN SIGN</code>) の後に
    <code>!</code> (<code>U+0021</code>
    <code class="charname">EXCLAMATION MARK</code>) がありますが、
    その後が <code>--</code> でも <code>DOCTYPE</code> でもありません。

    <dl class="switch">
    <dt>注釈
      <dd>HTML では注釈は
      <code class="example">&lt;!--</code> (<code>&lt;!</code>
      の<em>直後</em>に <code>-</code> <em>2つ</em>)
      からはじまり、
      <code class="example">--></code> で終わらなければいけません。
      <code>&lt;!</code> だけや <code>&lt;!-</code> だけでは、
      ただしい注釈ではありません。
    <dt><code>CDATA</code> 区間など、マーク付け区間
      <dd>HTML 要素内ではマーク付け区間は使えません。
    <dt>マーク付け宣言
      <dd>HTML 文書では <code>DOCTYPE</code> 宣言と注釈宣言を除き、
      マーク付け宣言は使えません。
    <dt>文字列 <code>&lt;!</code></dt>
      <dd>文字列 <code>&lt;!</code> を表したいときは、
      <code class="example">&amp;lt;!</code> とエスケープしないといけません。
    </dl>

!!!
<!DOCTYPE HTML><!$$HOGE>
!!!
<!DOCTYPE HTML><!DOCTYP$$O>
!!!XML
<!DOCTYPE x[<!$$z>]]>

* pio

name=tag-open-003f

@en
There is a processing instruction
@ja
処理指令があります

@en

<p>Processing instructions (<code>&lt;?<var>...</var>?></code>) and
the XML declaration (<code>&lt;?xml <var>...</var>?></code>) are not
allowed in HTML document.

    <dl class="switch">
    <dt><code>&lt;?xml?&gt;</code> (XML declaration)</dt>
        <dd>The XML declaration is not necessary in an HTML document.</dd>
    <dt><code>&lt;?xml-stylesheet?></code> (XML style sheet declaration)
        <dd>Use HTML <code>link</code> element with <code>rel</code>
        attribute set to <code>stylesheet</code> (or,
        <code>alternate stylesheet</code> for an alternative style 
        sheet).
          <pre class="example html"><code>&lt;link rel=stylesheet href="path/to/stylesheet.css"></code></pre>
        </dd>
    <dt><code>&lt;?php?&gt;</code> (<abbr>PHP</abbr> code)</dt>
        <dd>PHP is not supported.  It must be interpreted within
        server such that public documents must not contain any
        PHP code.
    <dt>Other processing instructions</dt>
        <dd>Processing instructions cannot be inserted in an HTML
        document.  If a processing instruction is required, use XML
        with an appropriate MIME type
        (e.g. <code>application/xhtml+xml</code>).
    </dl>

@ja

<p>処理指令 (<code>&lt;?<var>...</var>?></code>) や
XML 宣言 (<code>&lt;?xml <var>...</var>?></code>)
は HTML文書中では使えません。

    <dl class="switch">
    <dt><code>&lt;?xml?&gt;</code> (XML 宣言)</dt>
        <dd>XML 宣言は HTML 文書には不要です。
    <dt><code>&lt;?xml-stylesheet?></code> (XML スタイルシート宣言)
        <dd>HTML <code>link</code> 要素の <code>rel</code>
        属性を <code>stylesheet</code> (代替スタイルシートの場合は
        <code>alternate stylesheet</code>) にして使ってください。
          <pre class="example html"><code>&lt;link rel=stylesheet href="path/to/stylesheet.css"></code></pre>
        </dd>
    <dt><code>&lt;?php?&gt;</code> (<abbr>PHP</abbr> コード)</dt>
        <dd>PHP には対応していません。 PHP はサーバー内で処理するものですから、
        公開文書に PHP のコードが含まれていてはいけません。
    <dt>その他の処理指令
        <dd>処理指令は HTML 文書では使えません。
        処理指令が必要なときは、適切な MIME 型
        (<code>application/xhtml+xml</code> など) で XML
        を使ってください。
    </dl>

!!!
<$$?xml version="1.0"?><!DOCTYPE HTML>
!!!
<!DOCTYPE HTML><$$?php print ("Hello!")?>

* after head

name=after-head-start-b3lmnsstt
name=after-head-start-head

@en
There is a start-tag <code>&lt;<var>{text}</var>></code> after the
<code>&lt;/head></code> end tag
@ja
開始タグ <code>&lt;<var>{text}</var>></code> が終了タグ
<code>&lt;/head></code> より後にあります

@en

<p>There is a start tag of an element that should be a child of the
<code>head</code> element, or another <code>&lt;head></code> start
tag, after the <code>&lt;/head></code> end tag.

<p>Only white space characters and comments are allowed between the
<code>&lt;/head></code> end tag and the <code>&lt;body></code> start
tag.

@ja

<p><code>head</code> 要素の子供となるべき要素の開始タグが、
<code>&lt;/head></code> 終了タグよりも後にあります。

<p><code>&lt;/head></code> 終了タグと <code>&lt;body></code>
開始タグの間には空白文字と注釈しか書けません。

!!!
<!DOCTYPE html></head>$$<link>

* bare ero

module=Whatpm::WebVTT::Parser
name=character-reference-ws
name=character-reference-0022
name=character-reference-0026
name=character-reference-0027
name=character-reference-003c
name=character-reference-003d
name=character-reference-0060
name=character-reference-allowed-char

@en
There is an unescaped <code>&amp;</code> character
@ja
エスケープされていない <code>&amp;</code> があります

@en
    <p>An <code>&amp;</code> character which
    is not part of any reference appears in the input stream.</p>

    <p><em>Any <code>&amp;</code> character in URLs
    must be escaped as <code class="example">&amp;amp;</code>.</em></p>

    <p>The <code>&amp;</code> character must
    be the first character of a reference:
    </p>

      <dl class="switch">
      <dt>Named character reference (HTML / WebVTT)</dt>
          <dd><pre class="html example"><code>&amp;<var>entity-name</var>;</code></pre>
          where <var>entity-name</var> is the name of the
          character to be referenced.</dd>
      <dt>Numeric character reference (HTML only)</dt>
          <dd><pre class="html example"><code>&amp;#<var>d</var>;</code></pre>
          where <var>d</var> is the decimal representation of
          the code position of the character to be referenced.</dd>
      <dt>Hexadecimal character reference (HTML only)</dt>
          <dd><pre class="html example"><code>&amp;#x<var>h</var>;</code></pre>
          where <var>h</var> is the hexadecimal representation
          of the code position of the character to be referenced.</dd>
      </dl>

    <p>To represent <code>&amp;</code> as a data character, use
    named character reference:
    </p>
      <pre class="html example"><code>&amp;amp;</code></pre>

@ja
    <p>文字 <code>&amp;</code> が、文字参照の一部ではなく、
    単独で現れました。</p>

    <p><em>URL 中の <code>&amp;</code> 文字も
    <code class="example">&amp;amp;</code> 
    とエスケープしなければなりません。</em></p>

    <p>文字 <code>&amp;</code> は参照の最初の1文字でなければなりません。
    </p>

      <dl class="switch">
      <dt>名前付き文字参照 (HTML / WebVTT)</dt>
          <dd><pre class="html example"><code>&amp;<var>実体名</var>;</code></pre>
          の<var>実体名</var>が参照する文字の名前となります。</dd>
      <dt>数値文字参照 (HTML のみ)</dt>
          <dd><pre class="html example"><code>&amp;#<var>d</var>;</code></pre>
          の <var>d</var> が参照する文字の符号位置の十進表現となります。</dd>
      <dt>十六進数文字参照 (HTML のみ)</dt>
          <dd><pre class="html example"><code>&amp;#x<var>h</var>;</code></pre>
          の <var>h</var> が参照する文字の符号位置の十六進表現となります。</dd>
      </dl>

    <p>データ文字としての <code>&amp;</code> を表したい時は、
    文字参照を使ってください。
    </p>
      <pre class="html example"><code>&amp;amp;</code></pre>

!!!XML
<p x=A$$&=>

* bare nero

name=character-reference-number-else

@en
There is an <code>&amp;#</code> not followed by a code point number
@ja
<code>&amp;#</code> の後に符号位置の数値がありません

@en

<p>The string <code>&amp;#</code> is used to introduce a character
reference.  It must be followed by a decimal number or a
<code>x</code> character followed by a hexadecimal number,
representing the code point of a character, followed by a
<code>;</code> character.

<p>To represent a string <code>&amp;#</code>, the <code>&amp;</code>
character must be escaped as <code>&amp;amp;</code>.

@ja

<p>文字列 <code>&amp;#</code> は文字参照を表しています。
その後には文字の符号位置を表す十進数か、 <code>x</code>
と十六進数があって、その後に <code>;</code> が続かなければいけません。

<p><code>&amp;#</code> という文字列自体を表したいときは、 <code>&amp;</code>
は <code>&amp;amp;</code> と書かなければいけません。

!!!
<!DOCTYPE HTML>a&#$$
!!!
<!DOCTYPE HTML>a&#$$abx

* bare hcro

name=character-reference-before-hexadecimal-number-else

@en
There is an <code>&amp;#x</code> not followed by a code point number
@ja
<code>&amp;#x</code> の後に符号位置の数値がありません

@en

<p>The string <code>&amp;#x</code> is used to introduce a hexadecimal
character reference.  It must be followed by a hexadecimal number,
representing the code point of a character, followed by a
<code>;</code> character.

<p>To represent a string <code>&amp;#x</code>, the <code>&amp;</code>
character must be escaped as <code>&amp;amp;</code>.

@ja

<p>文字列 <code>&amp;#x</code> は十六進数文字参照を表しています。
その後には文字の符号位置を表す十六進数と <code>;</code> 
が続かなければいけません。

<p><code>&amp;#x</code> という文字列自体を表したいときは、 <code>&amp;</code>
は <code>&amp;amp;</code> と書かなければいけません。

!!!
<!DOCTYPE HTML>&#x$$
!!!
<!DOCTYPE HTML>&#X$$zza

* uppercase hcro

name=character-reference-number-0058

@en
The <code>x</code> character in the reference is in uppercase
@ja
文字参照の <code>x</code> が大文字になっています

@en
<p>In XML, the <code>x</code> character in <code>&amp;#<mark>x</mark><code>
introducing a hexadecimal character reference must be in
lowercase.
@ja
<p>XML では十六進数文字参照の最初の <code>&amp;#<mark>x</mark></code>
の <code>x</code> は小文字でなければいけません。

!!!XML
<a>&#$$X21;</a>

* no refc

name=character-reference-decimal-number-else
name=character-reference-hexadecimal-number-else
name=character-reference-name-003d
name=parameter-entity-name-in-markup-declaration-ws
name=parameter-entity-name-in-entity-value-double-quoted-0022
name=entity-value-double-quoted-state-character-reference-name-0022
name=parameter-entity-name-in-dtd-0025
name=parameter-entity-name-in-dtd-003c
name=parameter-entity-name-in-dtd-ws
name=character-reference-name-003c
name=character-reference-name-0026
name=character-reference-name-ws
name=character-reference-003e
name=character-reference-0025
name=character-reference-name-0022
name=character-reference-name-0025
name=character-reference-name-0027
name=character-reference-name-003e
name=character-reference-name-0060
name=character-reference-name-allowed-char
name=entity-value-double-quoted-state-character-reference-name-003d
name=entity-value-double-quoted-state-character-reference-name-0027
name=entity-value-double-quoted-state-character-reference-name-003c
name=entity-value-double-quoted-state-character-reference-name-0060
name=entity-value-double-quoted-state-character-reference-name-0025
name=entity-value-double-quoted-state-character-reference-name-ws
name=entity-value-single-quoted-state-character-reference-name-003d
name=entity-value-single-quoted-state-character-reference-name-0027
name=entity-value-single-quoted-state-character-reference-name-003c
name=entity-value-single-quoted-state-character-reference-name-0060
name=entity-value-single-quoted-state-character-reference-name-0022
name=entity-value-single-quoted-state-character-reference-name-0026
name=entity-value-single-quoted-state-character-reference-name-0025
name=entity-value-single-quoted-state-character-reference-name-ws
name=entity-value-double-quoted-state-character-reference-name-0026
name=entity-value-double-quoted-state-character-reference-name-0023
name=entity-value-double-quoted-state-character-reference-name-003e
name=entity-value-in-entity-state-character-reference-name-0022
name=entity-value-in-entity-state-character-reference-name-0025
name=entity-value-in-entity-state-character-reference-name-003e
name=entity-value-in-entity-state-character-reference-name-003c
name=entity-value-in-entity-state-character-reference-name-0060
name=entity-value-in-entity-state-character-reference-name-0023
name=entity-value-in-entity-state-character-reference-name-0026
name=entity-value-in-entity-state-character-reference-name-0027
name=entity-value-in-entity-state-character-reference-name-ws
name=entity-value-single-quoted-state-character-reference-name-0023
name=entity-value-single-quoted-state-character-reference-name-003e
name=entity-value-in-entity-state-character-reference-name-003d
name=character-reference-name-0023
name=parameter-entity-name-in-dtd-0023
name=parameter-entity-name-in-dtd-003e
name=parameter-entity-name-in-dtd-0026
name=parameter-entity-name-in-dtd-003d
name=parameter-entity-name-in-dtd-0060
name=parameter-entity-name-in-dtd-0027
name=parameter-entity-name-in-dtd-0022
name=parameter-entity-name-in-entity-value-double-quoted-0023
name=parameter-entity-name-in-entity-value-double-quoted-ws
name=parameter-entity-name-in-entity-value-double-quoted-003d
name=parameter-entity-name-in-entity-value-double-quoted-003e
name=parameter-entity-name-in-entity-value-double-quoted-003c
name=parameter-entity-name-in-entity-value-double-quoted-0060
name=parameter-entity-name-in-entity-value-double-quoted-0025
name=parameter-entity-name-in-entity-value-double-quoted-0026
name=parameter-entity-name-in-entity-value-double-quoted-0027
name=parameter-entity-name-in-entity-value-single-quoted-0023
name=parameter-entity-name-in-entity-value-single-quoted-ws
name=parameter-entity-name-in-entity-value-single-quoted-003d
name=parameter-entity-name-in-entity-value-single-quoted-003e
name=parameter-entity-name-in-entity-value-single-quoted-003c
name=parameter-entity-name-in-entity-value-single-quoted-0060
name=parameter-entity-name-in-entity-value-single-quoted-0025
name=parameter-entity-name-in-entity-value-single-quoted-0026
name=parameter-entity-name-in-entity-value-single-quoted-0027
name=parameter-entity-name-in-entity-value-single-quoted-0022
name=parameter-entity-name-in-entity-value-in-entity-0023
name=parameter-entity-name-in-entity-value-in-entity-0060
name=parameter-entity-name-in-entity-value-in-entity-003d
name=parameter-entity-name-in-entity-value-in-entity-003c
name=parameter-entity-name-in-entity-value-in-entity-0025
name=parameter-entity-name-in-entity-value-in-entity-0026
name=parameter-entity-name-in-entity-value-in-entity-0027
name=parameter-entity-name-in-entity-value-in-entity-0022
name=parameter-entity-name-in-entity-value-in-entity-003e
name=parameter-entity-name-in-entity-value-in-entity-ws
name=parameter-entity-name-in-markup-declaration-0022
name=parameter-entity-name-in-markup-declaration-0023
name=parameter-entity-name-in-markup-declaration-003e
name=parameter-entity-name-in-markup-declaration-0027
name=parameter-entity-name-in-markup-declaration-0060
name=parameter-entity-name-in-markup-declaration-0025
name=parameter-entity-name-in-markup-declaration-0026
name=parameter-entity-name-in-markup-declaration-003c
name=parameter-entity-name-in-markup-declaration-003d
name=parameter-entity-name-in-status-keyword-0022
name=parameter-entity-name-in-status-keyword-0025
name=parameter-entity-name-in-status-keyword-0026
name=parameter-entity-name-in-status-keyword-003e
name=parameter-entity-name-in-status-keyword-003d
name=parameter-entity-name-in-status-keyword-003c
name=parameter-entity-name-in-status-keyword-0060
name=parameter-entity-name-in-status-keyword-0027
name=parameter-entity-name-in-status-keyword-0023
name=parameter-entity-name-in-status-keyword-ws
layer=tokenizer
module=Web::HTML::Parser::tokenizer
module=Web::XML::Parser::tokenizer

@en
There is no <code>;</code> at the end of a reference
@ja
参照の末尾に <code>;</code> がありません

@en

<p>A character or entity reference must be terminated by a
<code>;</code> character.

@ja

<p>文字参照や実体参照の末尾には <code>;</code> が必要です。

!!!
<!DOCTYPE HTML>&#125$$abc;
!!!
<!DOCTYPE HTML>&#x125a$$zabc;
!!!
<!DOCTYPE HTML>&amp$$**
!!!
<!DOCTYPE HTML><a href="&amp$$=aa">
!!!XML
<p x="aa&copy$$="/>
!!!XML
<p x="aa&copy$$%"/>
!!!XML
<p x="aa&copy$$<"/>
!!!XML
<!DOCTYPE x [<!ENTITY %$$#>]><x/>

* invalid character reference

layer=character-set
module=Web::HTML::Parser::tokenizer
module=Web::XML::Parser::tokenizer

@en
Special code point <code><var>{text}</var></code> is referenced
@ja
特殊な符号位置 <code><var>{text}</var></code> が参照されています

@en

<p>A character reference cannot be used to reference a control
character, surrogate code point, noncharacter code point, or
non-Unicode code point.

@ja

<p>文字参照は、制御文字、サロゲート符号位置、非文字符号位置、
非 Unicode 符号位置を参照することはできません。

!!!
<!DOCTYPE HTML>$$&#0;
!text=U+0000
!!!
<!DOCTYPE HTML>$$&#x7F;
!text=U+007F
!!!
<!DOCTYPE HTML>$$&#x10FFFF;
!text=U+10FFFF
!!!
<!DOCTYPE HTML>$$&#x000000000000000700ffff;
!text=U-0700FFFF

* entity not declared

layer=entity
module=Web::HTML::Parser::tokenizer
module=Web::XML::Parser::tokenizer

@en
Entity <code><var>{value}</var></code> is not declared
@ja
実体 <code><var>{value}</var></code> は宣言されていません

@en
    <p>The referenced entity is not declared.</p>

    <dl class="swtich">
      <dt>In HTML document

      <dd>In an HTML document, only limited set of character
      references can be used.

      <dt>In XML document</dt>

      <dd>In an XML document, most of entities must be declared in the
      document type definition of the document.  Lack of entity
      declaration is validity error, or sometimes well-formedness
      error, depending on the standaloness of the document.</dd>

      <dd>In an XML document, although entities <code>amp</code>,
      <code>lt</code>, <code>gt</code>, <code>quot</code>, and
      <code>apos</code> are predefined, therefore they can be used
      even when they are not explicitly declared, they have to be
      declared for interoperability, according to the XML
      specification.</dd>

      <dt>In WebVTT document</dt>

      <dd>In a WebVTT document, only the following character
      references are allowed: <code>&amp;amp;</code>,
      <code>&amp;lt;</code>, <code>&amp;gt;</code>,
      <code>&amp;lrm;</code>, <code>&amp;rlm;</code>, and
      <code>&amp;nbsp;</code>.</dd>
    </dl>

<p>To represent an <code>&amp;</code> character, it must be escaped as
<code>&amp;amp;</code>.

@ja
    <p>参照されている実体は宣言されていません。</p>

    <dl class="swtich">
      <dt>HTML 文書

      <dd>HTML 文書では、限られた文字参照しか使えません。

      <dt>XML 文書</dt>

      <dd>XML 文書では、ほとんどの実体は文書の文書型定義で制限されていなければなりません。
      実体宣言が無いのは妥当性制約違反ですし、
      文書の単独性如何によっては整形式制約違反でもあります。</dd>

      <dd>XML 文書では、実体 <code>amp</code>,
      <code>lt</code>, <code>gt</code>, <code>quot</code>, 
      <code>apos</code> は定義済みであり、明示的に宣言する必要はないのですが、
      XML 仕様によれば相互運用性のために敢えて宣言する必要があります。</dd>

      <dt>WebVTT 文書</dt>

      <dd>WebVTT 文書では、文字参照として使えるのは
      <code>&amp;amp;</code>,
      <code>&amp;lt;</code>, <code>&amp;gt;</code>,
      <code>&amp;lrm;</code>, <code>&amp;rlm;</code>,
      <code>&amp;nbsp;</code> だけです。</dd>
    </dl>

<p><code>&amp;</code> という文字を表したいときは、
<code>&amp;amp;</code> とエスケープしなければいけません。

!!!
<!DOCTYPE HTML>$$&hoge;
!value=&hoge;

* parser:comment not closed

name=comment-end-0021
name=comment-end-002d
name=comment-end-else

@en
The comment contains <code>--</code>
@ja
注釈が <code>--</code> を含んでいます

@en

<p>A comment must be closed by <code>--></code>.  The comment's data
cannot contain <code>--</code>, even when it is not immediately
followed by a <code>></code> character.  The comment's data cannot end
with a <code>-</code> character.

@ja

<p>注釈は <code>--></code> で閉じなければいけません。
注釈のデータには、 <code>></code> が直後にないとしても、 <code>--</code>
を含めてはいけません。注釈のデータが <code>-</code>
で終わってはいけません。

!!!
<!DOCTYPE HTML><!--a--$$!>
!!!
<!DOCTYPE HTML><!--a--$$->
!!!
<!DOCTYPE HTML><!--a--$$b-->

* parser:comment closed

name=comment-start-003e
name=comment-start-dash-003e

@en
The comment is closed by <code>></code>
@ja
注釈が <code>></code> によって閉じられています

@en

<p>A comment must be closed by a <code>--></code>, whose
<code>--</code> must not be the <code>-</code>s in
<code>&lt;!--</code>.

<p>A comment's data cannot start with <code>></code> or
<code>-></code>.

@ja

<p>注釈は <code>--></code> で閉じなければいけませんが、その中の
<code>--</code> は <code>&lt;!--</code> の <code>-</code>
と重複していてはいけません。

<p>注釈のデータを <code>></code> や <code>-></code>
で始めることはできません。

!!!
<!DOCTYPE HTML><!--$$>
!!!
<!DOCTYPE HTML><!---$$>

* </br>

name=in-body-end-br

@en
There is a <code>&lt;/br></code> end tag
@ja
<code>&lt;/br></code> 終了タグがあります

@en

<p>A <code>&lt;/br></code> end tag is not allowed.  It is interpreted
as a start tag.

@ja

<p><code>&lt;/br></code> 終了タグは認められていません。
<code>&lt;/br></code> 終了タグは開始タグとみなされます。

!!!
<!DOCTYPE HTML>$$</br>

* </p>

name=in-body-end-p

@en
There is a <code>&lt;/p></code> end tag while there is no
opening <code>p</code> element
@ja
開いている <code>p</code> 要素はありませんが、 <code>&lt;/p></code> 
終了タグがあります

@en

<p>A <code>&lt;/p></code> end tag is not allowed unless there is an
opening <code>p</code> element.  It is interpreted as a start tag.

@ja

<p><code>&lt;/p></code> 終了タグは、開いている 
<code>p</code> 要素を閉じる時以外認められていません。
<code>&lt;/p></code> 終了タグは開始タグとみなされます。

!!!
<!DOCTYPE HTML><body>$$</p>

* in body:#eof

name=in-body-eof
name=in-frameset-eof
name=in-template-eof
name=text-eof
name=in-element-eof-2
name=in-element-eof
text=current node.local_name

@en
The <code><var>{text}</var></code> element is not closed
at the end of file
@ja
ファイルの末尾で <code><var>{text}</var></code>
要素が閉じられていません

@en

<p>Except for some exceptions (e.g. <code>body</code>,
<code>html</code>, <code>p</code>, and so on), all elements must be
explicitly closed by their end tags before the end of file.

@ja

<p>いくつかの例外 (<code>body</code>,
<code>html</code>, <code>p</code> など) を除き、
要素はすべてファイルの末尾までに終了タグで閉じなければいけません。

!!!
<!DOCTYPE HTML><hoge>$$
!text=hoge
!!!
<!DOCTYPE HTML><frameset>$$
!text=frameset
!!!
<!DOCTYPE HTML><template>$$
!text=template
!!!
<!DOCTYPE HTML><template><foo>$$
!text=foo
!!!
<!DOCTYPE HTML><xmp>$$
!text=xmp
!!!
<!DOCTYPE HTML><plaintext>a$$
!text=plaintext
!!!XML
<p>xxx
!text=p

* no root element

name=before-root-element-eof

@en
There is no root element
@ja
根要素がありません

@en
<p>There must be the root element's start tag in an XML document.
@ja
<p>XML 文書には根要素の開始タグが必要です。

!!!XML
$$
!!!XML
     $$

* duplicate body/html tag

name=in-body-start-body
name=in-body-start-html
value=tag_name

@en
There is a <code>&lt;<var>{value}</var>></code> start tag in the
<code>body</code> element
@ja
<code>body</code> 要素内に <code>&lt;<var>{value}</var>></code>
開始タグがあります

@en

<p>The HTML <code>html</code> and <code>body</code> elements cannot be
nested by themselves (directly or indirectly).  There must not be more
than one pairs of <code>&lt;html></code> and <code>&lt;body></code>
start tags in a document.

@ja

<p>HTML <code>html</code> 要素と HTML <code>body</code> 要素は 
(直接であれ間接であれ) それ自体を入れ子にできません。
文書中に <code>&lt;html></code> 開始タグや <code>&lt;body></code> 
開始タグが2組以上あってはいけません。

!!!
<!DOCTYPE HTML><body>$$<body>
!value=body
!!!
<!DOCTYPE HTML><body>$$<html>
!value=html

* parser:element not nestable

name=in-body-start-a
name=in-body-start-form
name=in-body-start-button
name=in-body-start-nobr
name=in-select-start-select
name=in-table-start-table
value=tag_name

@en
There is a start tag <code>&lt;<var>{value}</var>></code> in
an element <code><var>{value}</var></code>
@ja
<code><var>{value}</var></code> 要素内に <code>&lt;<var>{value}</var>></code>
開始タグがあります

@en

<p>HTML elements <code>a</code>, <code>button</code>,
<code>form</code>, and <code>nobr</code> cannot be nested.

  <dl class=switch>

  <dt><code>&lt;form></code>

  <dd>To associate some form controls in a <code>form</code> element
  with another form, use the <code>form</code> attribute of form
  controls.

  </dl>

@ja

<p>HTML 要素 <code>a</code>, <code>button</code>, <code>form</code>,
<code>nobr</code> を入れ子にすることはできません。


  <dl class=switch>

  <dt><code>&lt;form></code>

  <dd><code>form</code> 要素内のフォーム制御子を他のフォームと関連付けたいときは、
  フォーム制御子の <code>form</code> 属性を使ってください。

  </dl>

!!!
<!DOCTYPE HTML><a>$$<a>
!value=a
!!!
<!DOCTYPE HTML><form>$$<form>
!value=form
!!!
<!DOCTYPE HTML><button>$$<button>
!value=button
!!!
<!DOCTYPE HTML><nobr>$$<nobr>
!value=nobr
!!!
<!DOCTYPE HTML><select>$$<select>
!value=select
!!!
<!DOCTYPE HTML><table>$$<table>
!value=table

* in hn:hn

name=in-body-start-h6
value=tag_name
text=current node.local_name

@en
There is a start tag <code>&lt;<var>{value}</var>></code> in
an element <code><var>{text}</var></code>
@ja
<code><var>{text}</var></code> 要素内に <code>&lt;<var>{value}</var>></code>
開始タグがあります

@en

<p>HTML elements <code>h1</code>, <code>h2</code>, <code>h3</code>,
<code>h4</code>, <code>h5</code>, and <code>h6</code> cannot be
nested.

<p>To represent a subtitle or tagline, use the <code>hgroup</code>
element.

<p>To change font size, use CSS.

@ja

<p>HTML 要素 <code>h1</code>, <code>h2</code>, <code>h3</code>,
<code>h4</code>, <code>h5</code>, <code>h6</code>
を入れ子にすることはできません。

<p>副題やキャッチフレーズを記述したいときは、 <code>hgroup</code>
要素を使ってください。

<p>文字の大きさを変えたいときは CSS を使ってください。

!!!
<!DOCTYPE HTML><h3>$$<h5>
!value=h5
!text=h3

* in head:head

name=in-head-start-head

@en
There is a start tag <code>&lt;head></code> in the <code>head</code>
element
@ja
<code>head</code> 要素内に <code>&lt;head></code> 開始タグがあります

@en

<p>There is a start tag <code>&lt;head></code> in the HTML
<code>&lt;head></code> element.  In an HTML document there must not be
more than one <code>head</code> element.

@ja

<p>HTML <code>head</code> 要素内に <code>&lt;head></code>
開始タグがあります。 HTML 文書には <code>head</code>
要素が複数あってはなりません。

!!!
<!DOCTYPE HTML><head>$$<head>

* in body

name=in-body-start-c3fht6
name=in-body-start-frameset

@en
There is a start tag <code>&lt;<var>{text}</var>></code> in the
<code>body</code> element
@ja
<code>body</code> 要素内に <code>&lt;<var>{text}</var></code>
開始タグがあります

@en

<p>In an HTML <code>body</code> element, the start tag cannot be used.
The start tag is ignored.

  <dl class=switch>

  <dt><code>&lt;thead></code>, <code>&lt;tbody></code>,
  <code>&lt;tfoot></code>, <code>&lt;tr></code>, <code>&lt;th></code>,
  <code>&lt;td></code>, <code>&lt;caption></code>,
  <code>&lt;colgroup></code>, <code>&lt;col></code>

  <dd>These tags can only be used in a table.

  <dt><code>&lt;head></code>

  <dd>The <code>head</code> element can only be used before the
  <code>body</code> element.  There cannot be more than one
  <code>head</code> elements.

  <dt><code>&lt;frameset></code>, <code>&lt;frame></code>

  <dd>They are obsolete.  Use the <code>iframe</code> element instead.

  </dl>

@ja

<p>HTML <code>body</code> 要素内ではこの開始タグは使えません。
この開始タグは無視されます。

  <dl class=switch>

  <dt><code>&lt;thead></code>, <code>&lt;tbody></code>,
  <code>&lt;tfoot></code>, <code>&lt;tr></code>, <code>&lt;th></code>,
  <code>&lt;td></code>, <code>&lt;caption></code>,
  <code>&lt;colgroup></code>, <code>&lt;col></code>

  <dd>これらのタグは表の中でしか使えません。

  <dt><code>&lt;head></code>

  <dd><code>head</code> 要素は
  <code>body</code> 要素の前でしか使えません。文書中に
  <code>head</code> 要素を複数含めることはできません。

  <dt><code>&lt;frameset></code>, <code>&lt;frame></code>

  <dd>どちらも廃止されました。かわりに
  <code>iframe</code> 要素を使ってください。

  </dl>

!!!
<!DOCTYPE HTML><body>$$<tr>
!!!
<!DOCTYPE HTML><body>$$<col>
!!!
<!DOCTYPE HTML><body>$$<head>
!!!
<!DOCTYPE HTML><body>$$<frame>
!!!
<!DOCTYPE HTML><body>$$<frameset>
!!!
<!DOCTYPE HTML><body>$$<tbody>
!!!
<!DOCTYPE HTML><body>$$<td>

* image

name=in-body-start-image

@en
There is a start tag <code>&lt;image></code>
@ja
<code>&lt;image></code> 開始タグがあります

@en

<p>There is no <code>image</code> element, in fact.  Use the
<code>img</code> element instead.

@ja

<p>実のところ HTML に <code>image</code> 要素はありません。
<code>img</code> 要素を使ってください。

!!!
<!DOCTYPE HTML><body>$$<image>

* isindex

name=in-body-start-isindex

@en
There is a start tag <code>&lt;isindex></code>
@ja
<code>&lt;isindex></code> 開始タグがあります

@en

<p>The <code>isindex</code> element is obsolete.  Use the
<code>form</code> element instead.

@ja

<p><code>isindex</code> 要素は廃止されました。
<code>form</code> 要素を使ってください。

* in caption

name=in-caption-start-c3t6
text=tag_name

@en
There is a start tag <code>&lt;<var>{text}</var>></code>
in a table caption
@ja
表題内に  <code>&lt;<var>{text}</var>></code> 開始タグがあります

@en

    <p>In a <code>caption</code> element's <code>innerHTML</code>,
    start tags <code>&lt;caption></code>, <code>&lt;col></code>,
    <code>&lt;colgroup></code>, <code>&lt;tbody></code>,
    <code>&lt;td></code>, <code>&lt;tfoot></code>,
    <code>&lt;th></code>, <code>&lt;thead></code>, and
    <code>&lt;tr></code> are not allowed and are ignored.</p>

@ja

    <p><code>caption</code> 要素の <code>innerHTML</code> では開始タグ
    <code>&lt;caption></code>, <code>&lt;col></code>,
    <code>&lt;colgroup></code>, <code>&lt;tbody></code>,
    <code>&lt;td></code>, <code>&lt;tfoot></code>,
    <code>&lt;th></code>, <code>&lt;thead></code>,
    <code>&lt;tr></code> は使うことができず、無視されます。</p>

!!!
!context=caption
$$<tbody>
!text=tbody
!!!
!context=caption
$$<col>
!text=col

* in cell

name=in-cell-start-c3t6
text=tag_name

@en
There is a start tag <code>&lt;<var>{text}</var>></code>
in a table cell
@ja
表のこま内に  <code>&lt;<var>{text}</var>></code> 開始タグがあります

@en
    <p>A start tag that is not allowed in table cells (<code>td</code>
    or <code>th</code> elements) is used.</p>

    <p>In <code>innerHTML</code> of a <code>td</code> or
    <code>th</code> element, following start tags are not allowed:
    <code>&lt;caption></code>, <code>&lt;col></code>,
    <code>&lt;colgroup></code>, <code>&lt;tbody></code>,
    <code>&lt;td></code>, <code>&lt;tfoot></code>,
    <code>&lt;th></code>, <code>&lt;thead></code>, and
    <code>&lt;tr></code>.</p>

@ja
    <p>表のこま (<code>td</code> 要素や
    <code>th</code> 要素) 内では使えない開始タグがありました。</p>

    <p><code>td</code> や
    <code>th</code> の <code>innerHTML</code> では、次の開始タグは使えません:
    <code>&lt;caption></code>, <code>&lt;col></code>,
    <code>&lt;colgroup></code>, <code>&lt;tbody></code>,
    <code>&lt;td></code>, <code>&lt;tfoot></code>,
    <code>&lt;th></code>, <code>&lt;thead></code>,
    <code>&lt;tr></code>。</p>

* in colgroup

name=in-column-group-else

@en
There is a text or tag in a <code>colgroup</code> element
@ja
<code>colgroup</code> 要素内にテキストかタグがあります

@en
    <p>No element other than a <code>col</code> or
    <code>template</code> element or non-space character is not
    allowed in the <code>colgroup</code> element or in the
    <code>template</code> element containing <code>col</code>
    elements.</p>

@ja
    <p><code>col</code> と <code>template</code> 以外の要素や空白以外の文字は、
    <code>colgroup</code> 要素や、 <code>col</code> 要素を含んでいる
    <code>template</code> 要素の中では使えません。</p>

!!!
!context=colgroup
$$<p>
!!!
!context=colgroup
$$</p>
!!!
!context=colgroup
$$h

* in frameset

name=in-frameset-else

@en
There is a text or tag in a <code>frameset</code> element
@ja
<code>frameset</code> 要素内にテキストかタグがあります

@en
    <p>No element other than a <code>frameset</code> or
    <code>frame</code> element or non-space character is not
    allowed in the <code>frameset</code> element.

@ja
    <p><code>frameset</code> と <code>frame</code> 以外の要素や空白以外の文字は、
    <code>frameset</code> 要素の中では使えません。</p>

!!!
!context=frameset
$$<p>
!!!
!context=frameset
$$</p>
!!!
!context=frameset
$$h

* in noscript

name=in-head-noscript-else
name=in-head-noscript-start-head-noscript

@en
The <code>noscript</code> element is not closed before
a text, tag, or end of file
@ja
テキストやタグ、ファイルの末尾の前で <code>noscript</code>
要素が閉じられていません

@en

<p>Within a <code>noscript</code> element in a <code>head</code>
element, only limited set of HTML elements such as <code>link</code>
and <code>meta</code> can be used.

@ja

<p><code>head</code> 要素内の <code>noscript</code> 要素内では、
<code>link</code> や <code>meta</code> など一部の HTML
要素しか使えません。

!!!
!noscript
<!DOCTYPE HTML><head><noscript>$$<p>
!!!
!noscript
<!DOCTYPE HTML><head><noscript>$$<hoge>
!!!
!noscript
<!DOCTYPE HTML><head><noscript>$$foo
!!!
!noscript
<!DOCTYPE HTML><head><noscript>$$
!!!
!noscript
<!DOCTYPE HTML><head><noscript>$$<noscript></noscript>
!!!
!noscript
<!DOCTYPE HTML><head><noscript>$$<head></noscript>
!!!
!noscript
<!DOCTYPE HTML><head><noscript>$$<body>

* in table row

name=in-row-start-c3t4
value=tag_name

@en
There is a start tag <code>&lt;<var>{value}</var>></code> in a
table row
@ja
表の行内に <code>&lt;<var>{value}</var>></code> 開始タグがあります

@en

<p>In the <code>innerHTML</code> of an HTML <code>tr</code> element,
HTML elements <code>caption</code>, <code>colgroup</code>,
<code>col</code>, <code>thead</code>, <code>tbody</code>,
<code>tfoot</code>, and <code>tr</code> are not allowed.

@ja

<p>HTML <code>tr</code> 要素の <code>innerHTML</code> では、
HTML 要素 <code>caption</code>, <code>colgroup</code>,
<code>col</code>, <code>thead</code>, <code>tbody</code>,
<code>tfoot</code>, <code>tr</code> は使えません。

!!!
!context=tr
$$<tbody>
!value=tbody
!!!
!context=template
<td></td>$$<tbody>
!value=tbody

* in table body

name=in-table-body-start-c3t3
value=tag_name

@en
There is a start tag <code>&lt;<var>{value}</var>></code> in a
table row group
@ja
表の行群内に <code>&lt;<var>{value}</var>></code> 開始タグがあります

@en

<p>In the <code>innerHTML</code> of an HTML <code>thead</code>,
<code>tbody</code>, or <code>tfoot</code> element, HTML elements
<code>caption</code>, <code>colgroup</code>, <code>col</code>,
<code>thead</code>, <code>tbody</code>, and <code>tfoot</code> are not
allowed.

@ja

<p>HTML <code>thead</code> 要素、 HTML <code>tbody</code> 要素、
HTML <code>tfoot</code> 要素の <code>innerHTML</code> では、
HTML 要素 <code>caption</code>, <code>colgroup</code>,
<code>col</code>, <code>thead</code>, <code>tbody</code>,
<code>tfoot</code> は使えません。

!!!
!context=tbody
$$<tbody>
!value=tbody

* in select

name=in-select-else

@en
There is a tag in a <code>select</code> element
@ja
<code>colgroup</code> 要素内にタグがあります

@en

<p>In an HTML <code>select</code> element, only HTML
<code>option</code> and <code>optgroup</code> elements can be used.

@ja

<p>HTML <code>select</code> 要素内では HTML
<code>optgroup</code> 要素と HTML <code>option</code>
要素しか使えません。

!!!
<!DOCTYPE HTML><Select>$$<foo>
!!!
<!DOCTYPE HTML><Select>$$</foo>

* in select in table:end tag

name=in-select-in-table-end-ct7
value=tag_name

@en
The <code>select</code> element is not closed before
an end tag <code>&lt;/<var>{value}</var>></code>
@ja
<code>&lt;/<var>{value}</var>></code> 終了タグの前に
<code>select</code> 要素が閉じられていません

@en

<p>An HTML <code>select</code> element must be closed before a
table-related element is closed.

@ja

<p>HTML <code>select</code> 要素は、
表関係の要素が閉じられる前に閉じられていなければいけません。

!!!
<!DOCTYPE HTML><table><select>$$</tbody>
!value=tbody

* in select in table:start tag

name=in-select-in-table-start-ct7
value=tag_name

@en
The <code>select</code> element is not closed before
a start tag <code>&lt;<var>{value}</var>></code>
@ja
<code>&lt;<var>{value}</var>></code> 開始タグの前に
<code>select</code> 要素が閉じられていません

@en

<p>An HTML <code>select</code> element must be closed before a
table-related element is opened.

@ja

<p>HTML <code>select</code> 要素は、
表関係の要素が開かれる前に閉じられていなければいけません。

!!!
<!DOCTYPE HTML><table><select>$$<tbody>
!value=tbody

* no <tr>

name=in-table-body-start-td-th

@en
There is the start tag of a table cell without a <code>tr</code> element
@ja
<code>tr</code> 要素なしで表のこまの開始タグがあります

@en

<p>The start tag <code>&lt;td></code> or <code>&lt;th></code> must be
in an HTML <code>tr</code> element.

@ja

<p>開始タグ <code>&lt;td></code> や開始タグ <code>&lt;th></code>
の前に HTML <code>tr</code> 要素が開いていなければいけません。

!!!
<!DOCTYPE HTML><table>$$<td>

* in table

name=in-table-else
name=in-table-start-form
name=in-table-start-input

@en
There is a tag in a <code>table</code> element
@ja
<code>table</code> 要素内にタグがあります

@en

<p>Tags not related to tables are not allowed in an HTML
<code>table</code> element.  It must be put in an HTML
<code>caption</code> element, or in an HTML <code>td</code> or
<code>th</code> element, or must be moved outside of the table.

@ja

<p>表と関係のないタグを HTML の <code>table</code>
要素に含めることはできません。 HTML の
<code>caption</code>, <code>td</code>, <code>th</code>
のいずれかの要素に含めるか、表の外に出さなければいけません。

!!!
<!DOCTYPE HTML><table>$$<p>
!!!
<!DOCTYPE HTML><table>$$</gfoo>
!!!
<!DOCTYPE HTML><table>$$<form>
!!!
<!DOCTYPE HTML><table>$$<input>

* in table:#text

name=in-table-text-else
name=in-table-char

@en
There is a text in a <code>table</code> element
@ja
<code>table</code> 要素内にテキストがあります

@en

<p>Text other than white space characters is not allowed in an HTML
<code>table</code> element.  It must be put in an HTML
<code>caption</code> element, or in an HTML <code>td</code> or
<code>th</code> element, or must be moved outside of the table.

@ja

<p>空白文字以外のテキストを HTML の <code>table</code>
要素に含めることはできません。テキストは HTML の
<code>caption</code>, <code>td</code>, <code>th</code>
のいずれかの要素に含めるか、表の外に出さなければいけません。

!!!
<!DOCTYPE HTML><table>$$abc
!!!
<!DOCTYPE HTML><table><p>$$abc

* foreign:bad xmlns value

layer=namespaces
module=Web::HTML::Parser::tree_constructor

@en
The <code>xmlns</code> attribute value is incorrect
@ja
<code>xmlns</code> 属性値が正しくありません

@en

<p>The value of the <code>xmlns</code> attribute in the XMLNS
namespace must be equal to the namespace of the element.

<p>For an SVG element, the attribute value must be
<code>http://www.w3.org/2000/svg</code>.

<p>For a MathML element, the attribute value must be
<code>http://www.w3.org/1998/Math/MathML</code>.

<p>The value of the <code>xlink</code> attribute in the XMLNS
namespace (i.e. the <code>xmlns:xlink</code> attribute) must be
<code>http://www.w3.org/1999/xlink</code>.

@ja

<p>XMLNS 名前空間の <code>xmlns</code> 属性の値は、
要素の名前空間と同じでなければいけません。

<p>SVG 要素の属性値は
<code>http://www.w3.org/2000/svg</code> でなければいけません。

<p>MathML 要素の属性値は
<code>http://www.w3.org/1998/Math/MathML</code> でなければいけません。

<p>XMLNS 名前空間の <code>xlink</code> 属性
(<code>xmlns:xlink</code> 属性) の値は
<code>http://www.w3.org/1999/xlink</code> でなければいけません。

!!!
<!DOCTYPE HTML><svg $$xmlns=hoge>
!value=hoge
!!!
<!DOCTYPE HTML><math $$xmlns=hoge>
!value=hoge
!!!
<!DOCTYPE HTML><math><foo $$xmlns=hoge>
!value=hoge
!!!
<!DOCTYPE HTML><svg $$xmlns:xlink="foo">
!value=foo

* entity:in EMPTY

layer=entity
module=Web::XML::Parser::tokenizer

@en
An <code>EMPTY</code> element contains an entity reference
@ja
<code>EMPTY</code> 要素が実体参照を含んでいます

@en
<p>An element whose type is declared as <code>EMPTY</code>
cannot contain entity references.
@ja
<p><code>EMPTY</code> と宣言された要素型の要素は実体参照を含むことができません。

* VC:Element Valid:charref in element content

layer=entity
module=Web::XML::Parser::tokenizer

@en
An element-content element contains a character reference
@ja
要素内容として宣言された要素が文字参照を含んでいます

@en
<p>An element whose type is declared with an element content
(i.e. a content model group with no <code>#PCDATA</code>
keyword) cannot contain character references.
@ja
<p>要素内容 (<code>#PCDATA</code> 無しの内容モデル群) 
が宣言された要素型の要素は文字参照を含むことができません。

* charref in charset

layer=entity
module=Web::HTML::Parser::tokenizer

@en
The character encoding declaration contains a character reference
@ja
文字符号化宣言に文字参照が使われています

@en
    <p>The character encoding declaration cannot contain any HTML
    character reference.</p>

@ja
    <p>文字符号化宣言は HTML の文字参照を含んではいけません。</p>

!!!
<!DOCTYPE HTML><meta $$charset=utf-&#x38;>
!!!
<!DOCTYPE HTML><meta $$charset=utf&#46;8>
!!!
<!DOCTYPE HTML><meta $$charset=utf&amp;8>
!!!
<!DOCTYPE HTML><meta $$charset=utf&amp>
!!!
<!DOCTYPE HTML><meta http-equiv=content-type $$content="text/html;&#x20;charset=utf-8">

* text outside of root element

name=before-doctype-char
name=before-root-element-char
name=after-root-element-char

@en
There is a character outside of the root element
@ja
根要素の外に文字があります

@en
<p>Characters other than white spaces must be included in an
element or an attribute value.

@ja
<p>空白以外の文字は要素や属性値の中でしか使えません。

!!!XML
$$a<p/>
!!!XML
<!DOCTYPE p>$$a<p/>
!!!XML
<!---->  $$x<a/>
!!!XML
<a/>$$b

* ref outside of root element

layer=entity
module=Web::XML::Parser::tokenizer

@en
There is a reference outside of the root element
@ja
根要素の外に参照があります

@en
<p>Character or entity reference must be included in an element or
an attribute value.

@ja
<p>文字参照や実体参照は要素や属性値の中でしか使えません。

!!!XML
$$&amp;
!value=&amp;
!!!XML
<a/>$$&hoge;
!value=&hoge;

* second root element

name=after-root-element-start-else

@en
There is an element after the root element
@ja
根要素の後に別の要素があります

@en
<p>There must be only one root element.  No start tag is allowed
after the end tag of the root element.
@ja
<p>根要素は1つだけです。根要素の終了タグの後に開始タグは使えません。

!!!XML
<p/>$$<p>

* WFC:No Recursion

layer=entity
module=Web::XML::Parser::tokenizer

@en
An entity is recursively referenced
@ja
同じ実体が再帰的に参照されています

@en
<p>An entity cannot be referenced recursively.
@ja
<p>実体を再帰的に参照することはできません。

!!!XML
<!DOCTYPE p[
  <!ENTITY foo "$$&foo;">
]><p>&foo;
!value=&foo;

* entity:too deep

layer=entity
module=Web::XML::Parser::tokenizer

@en
Entities are nesting too many times
@ja
実体参照が深すぎます

@en
<p>Level of entity reference nesting is too deep.
<p>No more entity reference in this subtree is expanded for security reasons.
@ja
<p>実体参照の入れ子の階層が深すぎます。
<p>この部分木の実体参照は安全のためこれ以上展開しません。

* entity:too many refs

layer=entity
module=Web::XML::Parser::tokenizer

@en
There are too many entity references
@ja
実体参照が多すぎます

@en
<p>There are too many entity references.
<p>No more entity reference is expanded for security reasons.
@ja
<p>実体参照が多すぎます。
<p>実体参照は安全のためこれ以上展開しません。

* WFC:No External Entity References

layer=entity
module=Web::XML::Parser::tokenizer

@en
An external entity is referenced in an attribute value
@ja
属性値から外部実体が参照されています

@en
<p>An external entity cannot be referenced in an attribute value.

@ja
<p>属性値では外部実体を参照できません。

!!!XML
<!DOCTYPE a[<!ENTITY x SYSTEM "foo">]><a b="$$&x;">
!value=&x;

* VC:Standalone Document Declaration:attr

layer=dtd-augmentation
module=Web::XML::Parser::tree_constructor

@en
Attributes are affected by an external <code>ATTLIST</code> declaration
@ja
属性が外部実体の <code>ATTLIST</code> 宣言の影響を受けます

@en
<p>In an XML document with <code>standalone="yes"</code>,
element attributes must not be affected by <code>ATTLIST</code>
declarations in an external entity.

@ja
<p><code>standalone="yes"</code> が指定された XML 文書では、
要素の属性が外部実体で宣言された <code>ATTLIST</code>
宣言の影響を受けてはいけません。

* VC:Standalone Document Declaration:entity

layer=entity
module=Web::XML::Parser::tokenizer

@en
An externally declared entity is referenced in a standalone document
@ja
単独文書から外部実体で宣言された実体が参照されています

@en
<p>In an XML document with <code>standalone="yes"</code>,
an externally declared entity cannot be referenced.

@ja
<p><code>standalone="yes"</code> が指定された XML 文書では外部実体で宣言された実体を参照できません。

!!!XML
<?xml version="1.0" standalone="yes"?><!DOCTYPE html [
<!ENTITY % x PUBLIC "-//W3C//DTD XHTML 1.1//EN" "x">
%x;
]><p>$$&copy;</p>
!value=&copy;

* unparsed entity

layer=entity
module=Web::XML::Parser::tokenizer

@en
An unparsed entity is referenced
@ja
非解析対象実体が参照されています

@en
<p>An unparsed entity cannot be referenced.
@ja
<p>非解析対象実体は参照できません。

!!!XML
<!DOCTYPE a[
  <!ENTITY xy SYSTEM "x" NDATA foo>
]><p>$$&xy;</p>
!value=&xy;

* xml:dtd:entity value:unparsed entref

layer=entity
module=Web::XML::Parser::tokenizer

@en
An unparsed entity is referenced
@ja
非解析対象実体が参照されています

@en
<p>An unparsed entity cannot be referenced.
@ja
<p>非解析対象実体は参照できません。

!!!XML
<!DOCTYPE a[
  <!ENTITY xy SYSTEM "x" NDATA foo>
  <!ENTITY bar "a$$&xy;a">
]><p></p>
!value=&xy;
!level=w

* external entref not expanded

layer=entity
module=Web::XML::Parser::tokenizer

@en
The external entity reference is not expanded
@ja
この外部実体参照は展開されませんでした

@en
<p>The external entity reference or the <code>DOCTYPE</code>
declaration's external subset is not expanded as the parser
is not configured to expand external entities or it can't
retrieve the referenced file.  Note that XML parsers are not
required to expand any external entity.  Use of external
entities is not interoperable.
@ja
<p>外部実体参照や <code>DOCTYPE</code> 宣言の外部部分集合が、
構文解析器の設定のため、あるいは参照されているファイルを取得できなかったため、
展開されませんでした。なお XML 構文解析器は外部実体を展開する義務はありませんから、
外部実体は相互運用性が高くありません。

!!!XML
<?xml version="1.0" standalone="yes"?><!DOCTYPE a[
  <!ENTITY ext SYSTEM "ext">
]><a>$$&ext;</a>
!value=&ext;
!level=w

* external entref

layer=entity
module=Web::XML::Parser::tokenizer

@en
An external entity is referenced
@ja
外部実体が参照されています

@en
<p>XML parsers are not required to expand external entity references or
the <code>DOCTYPE</code> declaration's external subset.  Use of external
entities is not interoperable.
@ja
<p>XML 構文解析器は外部実体参照や <code>DOCTYPE</code> 
宣言の外部部分集合を展開する義務はありません。
外部実体は相互運用性が高くありません。

!!!XML
<?xml version="1.0" standalone="yes"?><!DOCTYPE a[
  <!ENTITY ext SYSTEM "ext">
]><a>$$&ext;</a>
!value=&ext;
!level=i

* WFC:PEs in Internal Subset

layer=entity
module=Web::XML::Parser::tokenizer

@en
There is an entity reference in a markup declaration
@ja
マーク付け宣言内に実体参照があります

@en
<p>A parameter entity reference is not allowed in a markup declaration
in the internal subset or a parameter entity referenced from the
internal subset.
@ja
<p>引数実体参照は、内部部分集合や内部部分集合から参照されている引数実体では使えません。

!!!XML
<!DOCTYPE a[<!ENTITY % foo "x ''"><!ENTITY $$%foo;>]><a/>
!value=%foo;

* entref in attr has element

name=attribute-value-in-entity-003c
name=default-attribute-value-in-entity-003c
layer=entity
module=Web::XML::Parser::tokenizer

@en
There is a tag in an entity referenced in an attribute value
@ja
属性値で参照されたいる実体にタグがあります

@en
<p>None of tag, comment, processing instructions, declaration, or
section can be used in an entity referenced in an attribute value.
@ja
<p>属性値で参照する実体ではタグ、注釈、処理指令、宣言、
区間を使うことはできません。

!!!XML
<!DOCTYPE a[
<!ENTITY x "<y></y>">
]><a b="$$&x;"/>
!value=&x;

* stop processing

layer=entity
module=Web::XML::Parser::tokenizer

@en
Processing of markup declarations is suppressed due to unread
entity reference
@ja
読めない実体参照があったためマーク付け宣言の処理を停止します

@en
<p>Since there is a reference whose entity cannot be read and
processed, further processing of markup declarations in the DTD is
suppressed.
@ja
<p>参照されている実体を読んで処理できなかったため、以後の DTD
中のマーク付け宣言の処理は行いません。

!!!XML
<!DOCTYPE hoge[
  <!ENTITY % x SYSTEM "f">
  $$%x;
  <!ENTITY hoge "">
]><hoge/>
!level=i
!!!XML
<!DOCTYPE hoge[
  $$%x;
  <!ENTITY hoge "">
]><hoge/>
!level=i

* condsect in internal subset

name=doctype-markup-declaration-open-005b

@en
There is a marked section in the internal subset
@ja
内部部分集合にマーク区間があります

@en
<p>In the internal subset or a parameter entity referenced from the internal
subset, no marked section is allowed.
@ja
<p>内部部分集合や内部部分集合から参照された引数実体では、
マーク区間は使えません。

!!!XML
<!DOCTYPE x[$$<![INCLUDE[aa]]>]><x/>

* ms:no status keyword

name=after-mss-003c
name=after-mss-005b
name=after-mss-003e
name=after-mss-005d

@en
There is no status keyword
@ja
状態キーワードがありません

@en
<p>There must be a status keyword (<code>INCLUDE</code> or <code>IGNORE</code>)
as the parameter to the marked section.
@ja
<p>マーク区間の引数として状態キーワード (<code>INCLUDE</code> か
<code>IGNORE</code>) が必要です。

* ms:no dso

name=after-include-keyword-003c
name=after-ignore-keyword-003c
name=after-ignore-keyword-005d
name=after-ignore-keyword-003e
name=after-include-keyword-005d
name=after-include-keyword-003e
name=after-ignore-keyword-else
name=after-include-keyword-else
name=after-mss-else

@en
The marked section is incompletly opened
@ja
マーク区間が完全には開いていません

@en
<p>A marked section must be opened by <code>&lt;![</code>,
followed by a status keyword, followed by a <code>[</code>.
@ja
<p>マーク区間は <code>&lt;![</code>、状態キーワード、 <code>[</code>
で開かなければいけません。

* unmatched mse

name=after-msc-003e

@en
There is an unmatched <code>]]></code>
@ja
対応していない <code>]]></code> があります

@en
<p>In XML, the <code>></code> character in <code>]]></code> must be
escaped as <code>&amp;gt;</code> unless it is closing a <code>CDATA</code>
section.
@ja
<p>XML では <code>]]></code> の <code>></code> は <code>CDATA</code>
区間を閉じる場合を除き <code>&amp;gt;</code> とエスケープしないといけません。

!!!XML
<p>$$]]></p>

* xml:CDATA section not allowed by cm

layer=dtd
module=Web::XML::Parser::tokenizer

@en
An <code>EMPTY</code> or element-content element contains a <code>CDATA</code>
section
@ja
<code>EMPTY</code> または要素内容を持つ要素が <code>CDATA</code>
区間を含んでいます

@en
<p>An element whose type is declared with the <code>EMPTY</code> keyword
or an element content (i.e. a content model group with no <code>#PCDATA</code>
keyword) cannot contain <code>CDATA</code> sections.
@ja
<p><code>EMPTY</code> キーワードまたは要素内容 (<code>#PCDATA</code>
キーワード無しの内容モデル群) を指定して宣言された要素型の要素では、
<code>CDATA</code> 区間は使えません。

* unclosed literal

layer=tokenizer
module=Web::XML::Parser::tokenizer

@en
There is an unclosed literal
@ja
引用符の後引用符で閉じられていません

@en
<p>The quoted string must be closed by matching quotation mark.
@ja
<p>引用符で始まった文字列は引用符で閉じなければいけません。

* unclosed cmgroup

name=before-content-model-item-003e
name=after-content-model-item-003e
layer=tokenizer
module=Web::XML::Parser::tokenizer

@en
There is an unclosed content model group
@ja
内容モデル群が括弧で閉じられていません

@en
<p>Content model groups must be closed by <code>)</code>.
@ja
<p>内容モデル群は <code>)</code> で閉じなければいけません。

* xml:dtd:cm:bad mixed repetition

layer=dtd
module=Web::XML::Parser::tree_constructor

@en
The repetition operator is not <code>*</code>
@ja
反復演算子が <code>*</code> ではありません

@en
<p>The repetition operator of a mixed content must be <code>*</code>.
It can be omitted only when the only content is <code>#PCDATA</code>.
Operators <code>+</code> and <code>?</code> are not allowed.
@ja
<p>混合内容では反復演算子として <code>*</code> を使わなければいけません。
省略できるのは <code>#PCDATA</code> のみを含んでいる時です。
<code>+</code> と <code>?</code> は使えません。

* xml:dtd:cm:bad mixed separator

layer=dtd
module=Web::XML::Parser::tree_constructor

@en
The connector is not <code>|</code>
@ja
接続子が <code>|</code> ではありません

@en
<p>The connector in a mixed content must be <code>|</code>.  It can't be
<code>,</code>.
@ja
<p>混合内容の接続子は <code>|</code> でなければいけません。 <code>,</code>
は使えません。

* xml:dtd:cm:nested mixed group

layer=dtd
module=Web::XML::Parser::tree_constructor

@en
There is a nested group in a mixed content
@ja
混合内容に入れ子の群があります

@en
<p>A mixed content cannot contain nested groups.
@ja
<p>混合内容に入れ子に群を含めることはできません。

* xml:dtd:cm:mixed element with repetition

layer=dtd
module=Web::XML::Parser::tree_constructor

@en
A repetition operator is specified within a mixed content
@ja
混合内容で反復演算子が指定されています

@en
<p>In a mixed content, repetition operators are not allowed.
@ja
<p>混合内容では反復演算子は使えません。

* xml:dtd:cm:bad element separator

layer=dtd
module=Web::XML::Parser::tree_constructor

@en
Different connectors <code><var>{text}</var></code> and
<code><var>{value}</var></code> are used within a group
@ja
同じ群で <code><var>{text}</var></code> と
<code><var>{value}</var></code> の異なる接続子が使われています。

* xml:dtd:cm:entity begins with connector

layer=entity
module=Web::XML::Parser::tokenizer

@en
The first non-space string in the parameter entity is a connector
@ja
引数実体の最初の空白でない文字列が接続子です

@en
<p>The first non-space string in parameter entities referenced within
a content model group should not be a connector (<code>|</code> or <code>,</code>).
@ja
<p>内容モデル群内で参照されている引数実体の最初の空白以外の文字は、
接続子 (<code>|</code> や <code>,</code>) とするべきではありません。

* xml:dtd:cm:entity ends with connector

layer=entity
module=Web::XML::Parser::tokenizer

@en
The last non-space string in the parameter entity is a connector
@ja
引数実体の最後の空白でない文字列が接続子です

@en
<p>The last non-space string in parameter entities referenced within
a content model group should not be a connector (<code>|</code> or <code>,</code>).
@ja
<p>内容モデル群内で参照されている引数実体の最後の空白以外の文字は、
接続子 (<code>|</code> や <code>,</code>) とするべきではありません。

* xml:dtd:cm:empty entity

layer=entity
module=Web::XML::Parser::tokenizer

@en
The parameter entity is empty
@ja
この引数実体は空です

@en
<p>Parameter entities referenced within a content model group should
not be empty.
@ja
<p>内容モデル群で参照されている引数実体は空であるべきではありません。

* no XML decl

layer=tokenizer
module=Web::XML::Parser::tokenizer

@en
There is no XML declaration
@ja
XML 宣言がありません

@en
<p>The document entity should contain an XML declaration.
The other parsed entities should contain a text declaration.
@ja
<p>文書実体には XML 宣言を含めるべきです。
その他の解析対象実体にはテキスト宣言を含めるべきです。

!!!XML
$$<p/>
!level=s
!!!XML
$$  <p/>
!level=s
!!!XML
$$abc<p/>
!level=s

* attribute missing:version

layer=tokenizer
module=Web::XML::Parser::tokenizer

@en
There is no <code>version</code> attribute
@ja
<code>version</code> 属性がありません

@en
<p>There must be the <code>version</code> pseudo-attribute in
an XML declaration.
@ja
<p>XML 宣言には <code>version</code> 擬似属性が必要です。

!!!XML
<?$$xml?><a/>
!!!XML
<?xml $$encoding="utf-8"?><a/>
!!!XML
<?xml  $$encoding="utf-8"?><a/>

* attribute missing:encoding

layer=tokenizer
module=Web::XML::Parser::tokenizer

@en
There is no <code>encoding</code> attribute
@ja
<code>encoding</code> 属性がありません

@en
<p>There must be the <code>encoding</code> pseudo-attribute in
a text declaration.
@ja
<p>テキスト宣言には <code>encoding</code> 擬似属性が必要です。

* attribute not allowed:standalone

layer=tokenizer
module=Web::XML::Parser::tokenizer

@en
There is a <code>standalone</code> attribute
@ja
<code>standalone</code> 属性があります

@en
<p>The <code>standalone</code> pseudo-attribute is not allowed
in a text declaration.
@ja
<p>テキスト宣言では <code>standalone</code> 擬似属性は使えません。

* XML standalone:syntax error

layer=tokenizer
module=Web::XML::Parser::tokenizer

@en
The <code>standalone</code> value is neither <code>yes</code> nor
<code>no</code>
@ja
<code>standalone</code> 属性値が <code>yes</code> でも <code>no</code>
でもありません

@en
<p>The <code>standalone</code> pseudo-attribute value must be
<code>yes</code> or <code>no</code>.
@ja
<p><code>standalone</code> 擬似属性値は <code>yes</code> か
<code>no</code> でなければいけません。

!!!XML
<?xml version="1.0" standalone="$$true"?><a/>
!value=true

* bogus XML declaration

layer=tokenizer
module=Web::XML::Parser::tokenizer

@en
The XML declaration is syntactically broken
@ja
この XML 宣言は構文的に正しくありません

@en
<p>The XML or text declaration is syntactically wrong.  It has
stricter syntax than normal tags.
@ja
<p>XML 宣言やテキスト宣言が構文的に正しくありません。
通常のタグより構文が厳密になっています。

!!!XML
<?xml version="1.0" $$foo="bar"?><a/>

* tag in pe in markup declaration

name=text-declaration-in-markup-declaration-003e-
name=text-declaration-in-markup-declaration-eof-
name=text-declaration-in-markup-declaration-003c
name=text-declaration-in-markup-declaration-0021
name=text-declaration-in-markup-declaration-0000
name=text-declaration-in-markup-declaration-0000-
name=text-declaration-in-markup-declaration-0025
name=text-declaration-in-markup-declaration-0026
name=text-declaration-in-markup-declaration-0026-

@en
There is a tag in a parameter entity referenced in a markup declaration
@ja
マーク付け宣言から参照されている引数実体にタグがあります

@en
<p>A parameter entity referenced from a markup declaration can't contain a tag,
comment, declaration, section, or processing instruction.
@ja
<p>マーク付け宣言から参照されている引数実体にはタグ、注釈、宣言、区間、
処理指令を含めることはできません。

* string in internal subset

name=dtd-else
name=in-subset-char
name=after-doctype-internal-subset-003e-fragment
name=after-dtd-msc-else
name=after-dtd-msc-005d
layer=tokenizer
module=Web::XML::Parser::tokenizer

@en
There is an unexpected string in DTD
@ja
DTD 中に予期せぬ文字列があります

@en
<p>Nothing ohter than declarations, comments, and processing
instructions can be used in the internal subset.  In external
subset, sections are also allowed.
@ja
<p>内部部分集合では宣言、注釈、処理指令しか使えません。
外部部分集合では区間も使えます。

!!!XML
<!DOCTYPE a[
$$]]>
]><a/>
!!!XML
<!DOCTYPE a[ $$a]><a/>

* string after internal subset

name=after-doctype-internal-subset-else

@en
There is an unknown parameter after the internal subset
@ja
内部部分集合の後に未知の引数があります

@en
<p>Only the <code>></code> character is allowed after the internal subset.
@ja
<p>内部部分集合の後には <code>></code> しか書けません。

!!!XML
<!DOCTYPE x[]$$x><x/>

* xml:dtd:no group item

name=before-allowed-token-003e
name=before-allowed-token-007c
name=before-allowed-token-0029
name=before-content-model-item-0029
name=before-content-model-item-002a
name=before-content-model-item-007c
name=before-content-model-item-002c
name=before-content-model-item-003f
name=before-content-model-item-002b

@en
No group item is specified
@ja
群に項目が指定されていません

@en
<p>There must be an item after <code>(</code>, <code>|</code>, or <code>,</code>.
@ja
<p><code>(</code>, <code>|</code>, <code>,</code> の後に項目がありません。

!!!XML
<!DOCTYPE a[<!ATTLIST x y ( $$) "x">]><a/>
!!!XML
<!DOCTYPE a[<!ATTLIST x y ( $$>]><a/>
!!!XML
<!DOCTYPE a[<!ATTLIST x y ( $$) "x">]><a/>
!!!XML
<!DOCTYPE a[<!ATTLIST x y ( $$|a) "y">]><a/>

* xml:dtd:no group delimiter

name=allowed-token-003e
name=after-allowed-token-else
name=after-allowed-token-003e
name=after-content-model-item-else
name=after-content-model-item-002b
name=after-content-model-item-0029
name=after-content-model-item-003f
name=after-content-model-group-else
name=after-content-model-item-002a
name=after-content-model-group-0028
name=after-content-model-item-0028
name=content-model-element-0028

@en
There is an unexpected character after a group item
@ja
群の項目の後に予期せぬ文字があります

@en
<p>There must be a delimiter (<code>|</code> or <code>,</code>) between group
items.  There cannot be a space within an allowed token or element name.
Nothing but <code>></code> is allowed after the outermost content model group.
@ja
<p>群の項目の間には区切子 (<code>|</code> か <code>,</code>) が必要です。
認められる字句や要素名の中には空白を入れられません。
最外内容モデル群の後には <code>></code> しか書けません。

!!!XML
<!DOCTYPE a[<!ATTLIST a b (ab $$cd) "X">]><a/>
!!!XML
<!DOCTYPE a[<!ATTLIST a b (ab $$>]><a/>
!!!XML
<!DOCTYPE a[<!ATTLIST a b (ab$$>]><a/>

* unmatched mgc

layer=tokenizer
module=Web::XML::Parser::tokenizer

@en
There is an unmatched <code>)</code>
@ja
対応しない <code>)</code> があります

@en
<p>There must be <code>(</code> before <code>)</code> in the
same entity.
@ja
同じ実体で <code>(</code> よりも後でないと <code>)</code> 
は使えません。

* xml:dtd:pi

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
There is a processing instruction in DTD
@ja
DTD 中に処理指令があります

@en
<p>Processing instructions in DTD are not interoperable.
@ja
<p>DTD 内の処理指令は相互運用性が高くありません。

!!!XML
<!DOCTYPE foo [$$<?hoge?>]><foo/>
!level=w

* no s after target

name=pi-target-question-else

@en
There is no space between the processing instruction's target and data
@ja
処理指令の対象名とデータの間に空白がありません

@en
<p>There must be a white space character between the target and data of
a processing instruction.
@ja
<p>処理指令の対象名とデータの間には空白が必要です。

!!!XML
<?hoge$$??>

* xml:pi:target not declared

layer=dtd
module=Web::XML::DTDValidator

@en
The target is not declared
@ja
対象が宣言されていません

@en
<p>The target of the processing instruction can be declared by
a <code>NOTATION</code> declaration whose name is the target.
@ja
<p>処理指令の対象は、名前が対象と同じ <code>NOTATION</code>
宣言により宣言することができます。

* xml:dtd:ext decl

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
There is a markup declaration in an external entity
@ja
外部実体にマーク付け宣言があります

@en
<p>Markup declarations in external entities are not interoperable.
@ja
<p>外部実体にあるマーク付け宣言は相互運用性が高くありません。

* no md name

name=attlist-003e
name=before-attlist-name-003e
name=notation-003e
name=before-entity-name-003e
name=entity-003e
name=element-003e
name=before-notation-name-003e
name=before-element-name-003e

@en
The markup declaration has no name
@ja
マーク付け宣言の名前がありません

@en
<p>A markup declaration must contain a name as its first parameter.
@ja
<p>マーク付け宣言には名前を最初の引数として含めなければいけません。

!!!XML
<!DOCTYPE a[<!ATTLIST$$>]><a/>

* no md def

name=after-entity-name-003e
name=notation-name-003e
name=after-notation-name-003e
name=entity-name-003e
name=element-name-003e
name=before-element-content-0029
name=before-element-content-007c
name=before-element-content-003f
name=before-element-content-002c
name=before-element-content-002a
name=before-element-content-002b
name=before-element-content-003e

@en
The markup declaration has no parameter
@ja
このマーク付け宣言には引数がありません

@en
<p>Markup declarations other than <code>ATTLIST</code> must contain
parameters after the name:
  <dl class=switch>
  <dt><code>ENTITY</code> declaration
  <dd>Either a system identifier and optional public identifier and notation name,
  or an entity value.
  <dt><code>NOTATION</code> declaration
  <dd>Public and/or system identifiers.
  <dt><code>ELEMENT</code> declaration
  <dd>The content model.
  </dl>
@ja
<p><code>ATTLIST</code> 以外のマーク付け宣言は名前の後に引数が必要です。
  <dl class=switch>
  <dt><code>ENTITY</code> 宣言
  <dd>システム識別子と省略可能な公開識別子や記法名か、実体値かのいずれか。
  <dt><code>NOTATION</code> 宣言
  <dd>公開識別子とシステム識別子の一方または両方。
  <dt><code>ELEMENT</code> 宣言
  <dd>内容モデル。
  </dl>

* xml:dtd:string after name

name=after-notation-name-else
name=after-entity-name-else

@en
There is an unknown parameter after the name
@ja
名前の後に未知の引数があります

@en
<p>Markup declarations other than <code>ATTLIST</code> must contain
parameters after the name:
  <dl class=switch>
  <dt><code>ENTITY</code> declaration
  <dd>Either a system identifier and optional public identifier and notation name,
  or an entity value.
  <dt><code>NOTATION</code> declaration
  <dd>Public and/or system identifiers.
  <dt><code>ELEMENT</code> declaration
  <dd>The content model.
  </dl>
@ja
<p><code>ATTLIST</code> 以外のマーク付け宣言は名前の後に引数が必要です。
  <dl class=switch>
  <dt><code>ENTITY</code> 宣言
  <dd>システム識別子と省略可能な公開識別子や記法名か、実体値かのいずれか。
  <dt><code>NOTATION</code> 宣言
  <dd>公開識別子とシステム識別子の一方または両方。
  <dt><code>ELEMENT</code> 宣言
  <dd>内容モデル。
  </dl>

!!!XML
<!DOCTYPE x[<!NOTATION foo $$bar>]><x/>

* string after md def

name=after-element-content-else
name=after-entity-parameter-else
name=after-content-model-item-007c
name=before-notation-system-identifier-else
name=after-ignore-keyword-003c-md-fragment
name=after-ignore-keyword-005b-md-fragment
name=after-ignore-keyword-005d-md-fragment
name=after-include-keyword-003c-md-fragment
name=after-include-keyword-005b-md-fragment
name=after-include-keyword-005d-md-fragment
name=bogus-status-keyword-005b-md-fragment
name=bogus-status-keyword-005d-md-fragment
name=bogus-status-keyword-003c-md-fragment
name=after-mss-003c-md-fragment
name=after-mss-005d-md-fragment
name=after-mss-005b-md-fragment
name=bogus-status-keyword-0025-md-fragment

@en
There is an unknown parameter in the markup declaration
@ja
マーク付け宣言に未知の引数があります

@en
<p>The markup declaration cannot contain an unknown parameter.
@ja
<p>マーク付け宣言に未知の引数を含めることはできません。

!!!XML
<!DOCTYPE x[<!ELEMENT foo ANY $$bar>]><x/>

* keyword-wrong-case

name=keyword-wrong-case

@en
The keyword is not in uppercase
@ja
キーワードが大文字ではありません

@en
<p>The keywords must be in uppercase in XML.
@ja
<p>XML ではキーワードは大文字でなければいけません。

!!!XML
<!DOCTYPE hoge [<!$$ELEment foo EMPTY>]><a/>
!value=ELEment

* xml:dtd:unknown content keyword

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
An unknown content model keyword is specified
@ja
未知の内容モデルのキーワードが指定されています

@en
<p>The content model keyword in an <code>ELEMENT</code> declaration
must be <code>EMPTY</code> or <code>ANY</code>.
@ja
<p><code>ELEMENT</code> 宣言の内容モデルのキーワードは
<code>EMPTY</code> か <code>ANY</code> でなければいけません。

!!!XML
<!DOCTYPE a[$$<!ELEMENT a foo>]><a/>
!value=foo

* Deterministic Content Models

layer=dtd
module=Web::XML::DTDValidator

@en
The content model is ambiguous
@ja
内容モデルが曖昧です

@en
<p>A content model has to be deterministic or unambiguous.
That is, it is not allowed for an element to be able to match
with multiple items in the content model.
@ja
<p>内容モデルは決定的で曖昧無いものでなければいけません。
すなわち、ある要素が内容モデルの複数の項目に一致する可能性があってはいけません。

* xml:dtd:element:no content model

layer=dtd
module=Web::XML::DTDValidator

@en
The element has no content model
@ja
要素の内容モデルがありません

@en
<p>To be valid, an element must be declared with its content model.
@ja
<p>妥当であるためには要素は内容モデルと共に宣言される必要があります。

* duplicate element decl

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
There is another declaration for the element
@ja
この要素の宣言が他にもあります

@en
<p>There must be at most one <code>ELEMENT</code> declaration
for the same tag name.
@ja
<p>同じタグ名の <code>ELEMENT</code> 宣言は高々1つしか含められません。

!!!XML
<!DOCTYPE a[
<!ELEMENT foo EMPTY>
$$<!ELEMENT foo ANY>
]><a/>
!value=foo

* xml:dtd:attlist ignored

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
The <code>ATTLIST</code> declaration is ignored
@ja
この <code>ATTLIST</code> 宣言は無視されます

@en
<p><code>ATTLIST</code> declarations are ignored if there is
an unread entity reference before the declaration.
@ja
<code>ATTLIST</code> 宣言はその前に読んでいない実体参照があると無視されます。

!!!XML
<!DOCTYPE a[
%foo;
  $$<!ATTLIST hoge>
]><a/>
!level=w

* duplicate attlist decl

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
There is another <code>ATTLIST</code> declaration for the element
@ja
同じ要素の <code>ATTLIST</code> 宣言が他にもあります

@en
<p>There should be at most one <code>ATTLIST</code> declaration
for the same tag name.
@ja
<p>同じタグ名の <code>ATTLIST</code> 宣言は高々1つしか含めるべきではありません。

!!!XML
<!DOCTYPE a[
  <!ATTLIST foo>$$<!ATTLIST foo>
]><a/>
!value=foo
!level=w

* empty attlist decl

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
The <code>ATTLIST</code> declaration is empty
@ja
この <code>ATTLIST</code> 宣言は空です

@en
<p>The <code>ATTLIST</code> declaration should contain
at least one attribute definition.
@ja
<p><code>ATTLIST</code> 宣言は属性定義を最低1つは含めるべきです。

!!!XML
<!DOCTYPE a[$$<!ATTLIST fa>]><a/>
!value=fa
!level=w

* xml:dtd:attlist element declared

layer=dtd
module=Web::XML::DTDValidator

@en
The element is not declared
@ja
要素が宣言されていません

@en
<p>The element used by the <code>ATTLIST</code> declaration
should be declared by an <code>ELEMENT</code> declaration.
@ja
<p><code>ATTLIST</code> 宣言で指定された要素は <code>ELEMENT</code>
宣言で宣言するべきです。

* unknown declared type

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
An unknown declared type keyword is specified
@ja
未知の宣言型キーワードが指定されています

@en
<p>The declared type must be one of allowed keywords.
@ja
<p>宣言型は認められているキーワードのいずれかを指定しなければいけません。

!!!XML
<!DOCTYPE a[
  <!ATTLIST  f  $$abc hoge "">
]><a/>
!value=hoge

* VC:No Duplicate Tokens

layer=dtd
module=Web::XML::DTDValidator

@en
The token is already specified
@ja
この字句は既に指定されています

@en
<p>Allowed tokens cannot contain duplication.
@ja
<p>認められる字句は異なるものでなければいけません。

* xml:dtd:duplicate nmtoken in element

layer=dtd
module=Web::XML::DTDValidator

@en
The token is also used by another attribute
@ja
この字句は他の属性で既に利用されています

@en
<p>Allowed tokens should not contain token allowed for another
attribute of the same element type.
@ja
<p>認められる字句には同じ要素型の他の属性で認められる字句を含めるべきではありません。

* default value not allowed

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
Default value is not allowed for the default type
@ja
この既定型では既定値は指定できません

@en
<p>The default value can't be specified when the default type
is <code>#IMPLIED</code> or <code>#REQUIRED</code>.
@ja
<p>既定値は既定型が <code>#IMPLIED</code> や <code>#REQUIRED</code>
の時には指定できません。

!!!XML
<!DOCTYPE a[
  <!ATTLIST foo $$bar CDATA #IMPLIED "a">
]><a/>

* default value not provided

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
Default value is not specified
@ja
既定値が指定されていません

@en
<p>The default value must be specified unless the default type
is <code>#IMPLIED</code> or <code>#REQUIRED</code>.
@ja
<p>既定型が <code>#IMPLIED</code> や <code>#REQUIRED</code>
の時以外は既定値を指定しなければなりません。

!!!XML
<!DOCTYPE a[
  <!ATTLIST foo $$bar CDATA #FIXED >
]><a/>

* unknown default type

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
An unknown default type keyword is specified
@ja
未知の既定型キーワードが指定されています

@en
<p>The default type must be one of allowed keywords.
@ja
<p>既定型は認められているキーワードのいずれかを指定しなければいけません。

!!!XML
<!DOCTYPE a[
  <!ATTLIST  f  $$abc hoge #hog>
]><a/>
!value=hog

* no attr type

name=attlist-attribute-name-003e
name=after-attlist-attribute-name-003e

@en
There is no attribute type
@ja
属性型がありません

@en
<p>There must be the attribute type (a declared type or a group of
allowed tokens) after the attribute name.
@ja
<p>属性名の後に属性型 (宣言型か認められる字句の群) が必要です。

!!!XML
<!DOCTYPE x[<!ATTLIST foo bar$$>]><x/>

* no attr default

name=before-attlist-attribute-default-ws
name=attlist-attribute-type-003e
name=after-attlist-attribute-type-003e
name=before-attlist-attribute-default-0025
name=after-allowed-token-list-003e
name=after-after-allowed-token-list-003e
name=before-attlist-attribute-default-0022
name=before-attlist-attribute-default-0027
name=before-attlist-attribute-default-003e
name=before-attlist-attribute-default-eof

@en
There is no attribute default
@ja
属性の既定値がありません

@en
<p>There must be the default type keyword (<code>#IMPLIED</code>,
<code>#REQUIRED</code>, or <code>#FIXED</code>) or the default value
after the declared type.
@ja
<p>宣言型の後には既定型キーワード (<code>#IMPLIED</code>,
<code>#REQUIRED</code>, <code>#FIXED</code>) か既定値が必要です。

!!!XML
<!DOCTYPE a[<!ATTLIST a b CDATA #$$ >]></a>
!!!XML
<!DOCTYPE a[<!ATTLIST a b CDATA$$>]></a>

* duplicate attrdef

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
There is another attribute definition with same name
@ja
同じ名前の属性定義が他にもあります

@en
<p>There should be at most one attribute definition for
same element and attribute names.  Anything other than
the first definition is ignored.
@ja
<p>同じ要素名と属性名の属性定義は高々1つしか含めるべきではありません。
最初の属性定義以外は無視されます。

!!!XML
<!DOCTYPE a[
  <!ATTLIST a bb CDATA "" $$bb ID #IMPLIED>
]><a/>
!value=bb
!level=w

* no notation name

name=before-ndata-identifier-003e
name=after-ndata-keyword-003e

@en
There is no notation name
@ja
記法名がありません

@en
<p>There must be a notation name after the <code>NDATA</code> keyword
@ja
<p>キーワード <code>NDATA</code> の後に記法名がありません。

!!!XML
<!DOCTYPE x[<!ENTITY foo SYSTEM "aa" NDATA $$>]><x/>

* xml:dtd:entity ignored

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
The <code>ENTITY</code> declaration is ignored
@ja
この <code>ENTITY</code> 宣言は無視されます

@en
<p>An <code>ENTITY</code> declaration is ignored if there
is an unread entity reference before the declaration.
@ja
<p><code>ENTITY</code> 宣言は、その前に読んでいない実体参照があると無視されます。

!!!XML
<!DOCTYPE a [
  %foo;
  $$<!ENTITY hoge "">
]><a/>
!level=w

* duplicate entity decl

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
There is another <code>ENTITY</code> declaration with
same type and name
@ja
同じ種類と名前の <code>ENTITY</code> 宣言が他にもあります

@en
<p>There should be at most one <code>ENTITY</code> declaration
with same type and name.  Anything other than the first declaration
is ignored.
@ja
<p>同じ種類と名前の <code>ENTITY</code> 宣言は高々1つしか含めるべきではありません。
最初の宣言以外は無視されます。

!!!XML
<!DOCTYPE a[
 <!ENTITY foo "">$$<!ENTITY foo "a">
]><a/>
!value=&foo;
!level=w
!!!XML
<!DOCTYPE a[
 <!ENTITY % foo "">$$<!ENTITY % foo "a">
]><a/>
!value=%foo;
!level=w

* bad predefined entity decl

layer=entity
module=Web::XML::Parser::tree_constructor

@en
The predefined entity declaration is wrong
@ja
この定義済み実体の宣言は正しくありません

@en
<p>The entity declaration for a predefined entity must be
equivalent to the predefined entity.
@ja
<p>定義済み実体の実体宣言は定義済み実体と等価なものでなければいけません。

!!!XML
<!DOCTYPE a[
  $$<!ENTITY amp "hoge">
]><a/>
!value=amp

* xml:dtd:param entity with ndata

layer=entity
module=Web::XML::Parser::tree_constructor

@en
The notation name is specified for a parameter entity
@ja
引数実体に記法名が指定されています

@en
<p>The notation name cannot be specified in a parameter entity declaration.
@ja
<p>引数実体宣言には記法名を指定できません。

!!!XML
<!DOCTYPE a[$$<!ENTITY % foo SYSTEM "bar" NDATA x>]><a/>
!value=%foo;

* duplicate notation decl

layer=tree-construction
module=Web::XML::Parser::tree_constructor

@en
There is another <code>NOTATION</code> declaration with the same name
@ja
同じ名前の <code>NOTATION</code> 宣言が他にもあります

@en
<p>There must be at most one <code>NOTATION</code> declaration
for the same name.
@ja
<p>同じ名前の <code>NOTATION</code> 宣言は高々1つしか含めるべきではありません。

!!!XML
<!DOCTYPE a[
  <!NOTATION foo SYSTEM "x">$$<!NOTATION foo SYSTEM "a">
]><a/>
!value=foo

* VC:Notation Attributes:declared

layer=dtd
module=Web::XML::DTDValidator

@en
The notation is not declared
@ja
記法が宣言されていません

@en
<p>The notation allowed for a notation attribute must be declared
by a <code>NOTATION</code> declaration.
@ja
<p>記法宣言で認められる記法は <code>NOTATION</code> 
宣言で宣言されていなければいけません。

* VC:Notation Declared

layer=dtd
module=Web::XML::DTDValidator

@en
The notation is not declared
@ja
記法が宣言されていません

@en
<p>The notation specified with the <code>NDATA</code> keyword
must be declared with a <code>NOTATION</code> declaration.
@ja
<p><code>NDATA</code> キーワードと共に指定された記法名は
<code>NOTATION</code> 宣言で宣言しなければいけません。

* mdc in pe in md

name=before-notation-name-003e-md-fragment
name=content-model-element-003e-md-fragment
name=element-content-keyword-003e-md-fragment
name=bogus-markup-declaration-003e-md-fragment
name=between-entity-public-and-system-identifiers-003e-md-fragment
name=entity-public-identifier-double-quoted-003e-md-fragment
name=entity-public-identifier-single-quoted-003e-md-fragment
name=before-entity-public-identifier-003e-md-fragment
name=after-entity-name-003e-md-fragment
name=before-entity-system-identifier-003e-md-fragment
name=after-ndata-keyword-003e-md-fragment
name=before-entity-name-003e-md-fragment
name=entity-name-003e-md-fragment
name=before-allowed-token-003e-md-fragment
name=after-attlist-attribute-type-003e-md-fragment
name=attlist-attribute-type-003e-md-fragment
name=attlist-attribute-name-003e-md-fragment
name=before-attlist-attribute-name-003e-md-fragment
name=attlist-name-003e-md-fragment
name=before-attlist-name-003e-md-fragment
name=after-entity-parameter-003e-md-fragment
name=after-allowed-token-list-003e-md-fragment
name=after-after-allowed-token-list-003e-md-fragment
name=after-allowed-token-003e-md-fragment
name=attlist-attribute-default-003e-md-fragment
name=after-attlist-attribute-name-003e-md-fragment
name=after-attlist-attribute-default-003e-md-fragment
name=after-attlist-default-value-003e-md-fragment
name=after-content-model-item-003e-md-fragment
name=after-content-model-group-003e-md-fragment
name=after-element-content-003e-md-fragment
name=before-content-model-item-003e-md-fragment
name=before-element-content-003e-md-fragment
name=before-element-name-003e-md-fragment
name=element-name-003e-md-fragment
name=after-entity-public-identifier-003e-md-fragment
name=after-entity-public-keyword-003e-md-fragment
name=after-entity-system-keyword-003e-md-fragment
name=after-entity-system-identifier-003e-md-fragment
name=entity-003e-md-fragment
name=element-003e-md-fragment
name=notation-003e-md-fragment
name=attlist-003e-md-fragment
name=ndata-identifier-003e-md-fragment
name=notation-public-identifier-double-quoted-003e-md-fragment
name=notation-public-identifier-single-quoted-003e-md-fragment
name=notation-name-003e-md-fragment
name=after-notation-public-identifier-003e-md-fragment
name=between-notation-public-and-system-identifiers-003e-md-fragment
name=after-notation-system-keyword-003e-md-fragment
name=after-notation-public-keyword-003e-md-fragment
name=before-notation-public-identifier-003e-md-fragment
name=before-notation-system-identifier-003e-md-fragment
name=after-notation-name-003e-md-fragment
name=after-notation-system-identifier-003e-md-fragment
name=before-ndata-identifier-003e-md-fragment
name=before-ndata-keyword-003e-md-fragment
name=allowed-token-003e-md-fragment
name=before-attlist-attribute-default-003e-md-fragment
name=after-ignore-keyword-003e-md-fragment
name=after-include-keyword-003e-md-fragment
name=after-mss-003e-md-fragment
name=bogus-status-keyword-003e-md-fragment

@en
There is a <code>></code> in a parameter entity
@ja
引数実体内に <code>></code> があります

@en
<p>The markup declaration cannot be closed within a parameter
entity referenced from the markup declaration.
@ja
<p>マーク付け宣言から参照されている引数実体でマーク付け宣言を閉じることはできません。

* xml:space:bad type

layer=dtd
module=Web::XML::DTDValidator

@en
The declared type of <code>xml:space</code> attribute is
not <code>(default|preserve)</code>
@ja
<code>xml:space</code> 属性の宣言型が <code>(default|preserve)</code>
ではありません

@en
<p>The declared type of the <code>xml:space</code> attribute
must be <code>(default | preserve)</code>, <code>(default)</code>,
or <code>(preserve)</code>.
@ja
<p><code>xml:space</code> 属性の宣言型は
<code>(default | preserve)</code>, <code>(default)</code>,
<code>(preserve)</code> のいずれかでなければいけません。

* VC:Element Valid:element children:text

layer=dtd
module=Web::XML::DTDValidator

@en
An element-content element contains a non-space character
@ja
要素内容の要素に空白以外の文字が含まれています

@en
<p>An element whose type is declared with a element content
(i.e. a content model group without <code>#PCDATA</code>)
cannot contain characters other than white spaces.
@ja
<p>要素内容 (<code>#PCDATA</code> 無しの内容モデル群)
で宣言された要素型の要素は、空白以外の文字を含むことができません。

* VC:Standalone Document Declaration:ws

layer=dtd
module=Web::XML::DTDValidator

@en
An element-content element contains a space character
@ja
要素内容の要素に空白が含まれています

@en
<p>In a standalone document, an element whose type is declared
with a element content (i.e. a content model group without
<code>#PCDATA</code>) cannot contain white spaces.
@ja
<p>単独文書では、要素内容 (<code>#PCDATA</code> 無しの内容モデル群)
で宣言された要素型の要素は空白を含むことができません。

* xml:name syntax

layer=tokenizer
module=Web::XML::DTDValidator

@en
This is not an XML <code>Name</code>
@ja
XML <code>Name</code> ではありません

@en
<p>XML element, attribute, entity, and notation names, as well as
processing instruction target and <code>ID</code>s, must be an
XML <code>Name</code>.  It must be a string of one or more XML
name characters.  Its first character must be an XML name start
character.
@ja
<p>XML の要素、属性、実体、記法の名前や、処理指令の対象名、
<code>ID</code> は XML <code>Name</code> でなければいけません。
XML 名前文字のみで構成される1文字以上の文字列で、
最初の文字は XML 名前開始文字でなければいけません。

* xml:names syntax

layer=dtd
module=Web::XML::DTDValidator

@en
This is not a space-separated list of XML <code>Name</code>s
@ja
XML <code>Name</code> の空白区切りのリストではありません

@en
<p>Attribute values whose type is <code>IDREFS</code> or <code>ENTITIES</code>
must be one or more XML <code>Name</code>s separated by
a U+0020 <code class=charname>SPACE</code> character.
The list cannot be empty.
@ja
<p>宣言型が <code>IDREFS</code> や <code>ENTITIES</code>
の属性の値は、 U+0020 <code class=charname>SPACE</code>
区切りの1つ以上の XML <code>Name</code> のリストでなければいけません。
空にはできません。

* xml:ncname syntax

layer=tokenizer
module=Web::XML::DTDValidator

@en
The <code>:</code> is not allowed
@ja
<code>:</code> を使うことはできません

@en
<p>XML entity and notation names, as well as
processing instruction target and <code>ID</code>s, must
not contain a <code>:</code> character.
@ja
<p>XML の実体や記法の名前や、処理指令の対象名、
<code>ID</code> は <code>:</code> を含めることができません。

* xml:qname syntax

layer=tokenizer
module=Web::XML::DTDValidator

@en
This is not an XML <code>QName</code>
@ja
XML <code>QName</code> ではありません

@en
<p>XML element and attribute names must be an XML <code>QName</code>, i.e.
a local name or a prefix followed by <code>:</code> and a local name.
<p>A prefix or local name must be a string of one or more XML
name characters.  Their first character must be an XML name start
character.  They cannot contain a <code>:</code> character.
@ja
<p>XML の要素や属性の名前は XML <code>QName</code> でなければいけません。
すなわち、局所名か、接頭辞と <code>:</code> と局所名のいずれかでなければいけません。
<p>接頭辞や局所名は、XML 名前文字のみで構成される1文字以上の文字列で、
最初の文字は XML 名前開始文字でなければいけません。また <code>:</code>
を含めてはいけません。

* xml:nmtoken syntax

layer=dtd
module=Web::XML::DTDValidator

@en
The value is not a string of name characters
@ja
名前文字の文字列ではありません

@en
<p>The value of an attribute whose type is <code>NMTOKEN</code>
can only contain XML name characters.  It cannot be the empty string.
@ja
<p>宣言型が <code>NMTOKEN</code> の属性の値は XML 名前文字しか含めることができません。
属性値は空にはできません。

* xml:nmtokens syntax

layer=dtd
module=Web::XML::DTDValidator

@en
The value is not a list of one or more strings of name characters
@ja
名前文字の文字列のリストではありません

@en
<p>The value of an attribute whose type is <code>NMTOKENS</code> must be
a list of one or more substrings that only contain XML name characters,
separated by a U+0020 <code class=charname>SPACE</code> character.
It cannot be the empty string.
@ja
<p>宣言型が <code>NMTOKENS</code> の属性の値は、1つ以上の XML 
名前文字のみの文字列の U+0020 <code class=charname>SPACE</code>
文字のリストでなければいけません。
属性値は空にはできません。

* VC:No Duplicate Types

layer=dtd
module=Web::XML::DTDValidator

@en
The element type is already specified
@ja
この要素型は既に指定されています

@en
<p>In a group, there must not be the same element type more than
once.
@ja
<p>群内に同じ要素型を二度指定することはできません。

* VC:Element Valid:declared

layer=dtd
module=Web::XML::DTDValidator

@en
The element type is not declared
@ja
要素型が宣言されていません

@en
<p>The element type must be declared by an <code>ELEMENT</code> declaration.
@ja
<p>要素型は <code>ELEMENT</code> 宣言で宣言しなければいけません。

* xml:dtd:cm:element not declared

layer=dtd
module=Web::XML::DTDValidator

@en
The element type is not declared
@ja
要素型が宣言されていません

@en
<p>The content model contains an element type that is not declared.
@ja
<p>内容モデルに宣言されていない要素型が含まれています。

* xml:dtd:non-id ID

layer=dtd
module=Web::XML::DTDValidator

@en
The <code>ID</code> attribute has name other than <code>id</code>
@ja
<code>ID</code> 属性の名前が <code>id</code> ではありません

@en
<p>For compatibility with DOM, the <code>ID</code>-typed attribute
should have the name <code>id</code>.
@ja
<p>DOM との互換性のため、 <code>ID</code> 型の属性の名前は
<code>id</code> とするべきです。

* xml:dtd:id non-ID

layer=dtd
module=Web::XML::DTDValidator

@en
The <code>id</code> attribute has a declared type other than <code>ID</code>
@ja
<code>id</code> 属性の宣言型が <code>ID</code> ではありません

@en
<p>For compatibility with DOM, the <code>id</code> attribute should
have the declared type <code>ID</code>.
@ja
<p>DOM との互換性のため、 <code>id</code> 属性の宣言型は
<code>ID</code> とするべきです。

* VC:ID Attribute Default

layer=dtd
module=Web::XML::DTDValidator

@en
An <code>ID</code> attribute cannot contain a default value
@ja
<code>ID</code> 属性の既定値は指定できません

@en
<p>An <code>ID</code>-typed attribute cannot have any default value.
@ja
<p><code>ID</code> 型の属性は既定値を持てません。

* VC:ID:unique

layer=dtd
module=Web::XML::DTDValidator

@en
There is anothor <code>ID</code> attribute with same value
@ja
他にも同じ値の <code>ID</code> 属性があります

@en
<p>The <code>ID</code> must be unique in the document
@ja
<p><code>ID</code> は文書内で固有の値でなければいけません。

* VC:IDREF:referenced element

layer=dtd
module=Web::XML::DTDValidator

@en
The specified element is not found
@ja
指定された要素がありません

@en
<p>A value of an attribute whose type is <code>IDREF</code> or 
<code>IDREFS</code> must be an <code>ID</code> of an element in the document.
@ja
<p>宣言型が <code>IDREF</code> や <code>IDREFS</code> の属性の値は、
文書中の要素の <code>ID</code> でなければいけません。

* VC:One ID per Element Type

layer=dtd
module=Web::XML::DTDValidator

@en
There are multiple <code>ID</code> attributes
@ja
<code>ID</code> 属性が複数あります。

@en
<p>There can be at most one <code>ID</code>-typed attribute.
@ja
<p><code>ID</code> 型の属性は高々1つしかあってはいけません。

* VC:One Notation per Element Type

layer=dtd
module=Web::XML::DTDValidator

@en
There are multiple <code>NOTATION</code> attributes
@ja
<code>NOTATION</code> 属性が複数あります。

@en
<p>There can be at most one <code>NOTATION</code>-typed attribute.
@ja
<p><code>NOTATION</code> 型の属性は高々1つしかあってはいけません。

* VC:No Notation on Empty Element

layer=dtd
module=Web::XML::DTDValidator

@en
A <code>NOTATION</code> attribute is declared on an <code>EMPTY</code> element
@ja
<code>NOTATION</code> 属性が <code>EMPTY</code> 要素に宣言されています

@en
<p>An element type whose content is <code>EMPTY</code> cannot have
a <code>NOTATION</code>-typed attribute.
@ja
<p>内容が <code>EMPTY</code> の要素型は <code>NOTATION</code>
型の属性を持つことができません。

* VC:Enumeration

layer=dtd
module=Web::XML::DTDValidator

@en
The value is not one of allowed tokens
@ja
この値は認められている字句ではありません

@en
<p>The value of an attribute whose type is a list of enumerated values
must be one of those values.
@ja
<p>列挙型の属性の値はそのいずれかでなければいけません。

* VC:Attribute Default Value Syntactically Correct:enumeration

layer=dtd
module=Web::XML::DTDValidator

@en
The value is not one of allowed tokens
@ja
この値は認められている字句ではありません

@en
<p>The value of an attribute whose type is a list of enumerated values
must be one of those values.
@ja
<p>列挙型の属性の値はそのいずれかでなければいけません。

* VC:Notation Attributes:enumeration

layer=dtd
module=Web::XML::DTDValidator

@en
The value is not one of allowed tokens
@ja
この値は認められている字句ではありません

@en
<p>The value of an attribute whose type is a list of enumerated values
must be one of those values.
@ja
<p>列挙型の属性の値はそのいずれかでなければいけません。

* VC:Entity Name:declared

layer=dtd
module=Web::XML::DTDValidator

@en
The entity is not declared
@ja
この実体は宣言されていません

@en
<p>The value specified by an <code>ENTITY</code> or <code>ENTITIES</code>
typed attribute must be a general entity name.
@ja
<p><code>ENTITY</code> 型や <code>ENTITIES</code> 型の属性で指定された値は、
一般実体名でなければいけません。

* VC:Entity Name:unparsed

layer=dtd
module=Web::XML::DTDValidator

@en
The entity is not an unparsed entity
@ja
この実体は非解析対象実体ではありません

@en
<p>The entity specified by an <code>ENTITY</code> or <code>ENTITIES</code>
typed attribute must be an unparsed entity.
@ja
<p><code>ENTITY</code> 型や <code>ENTITIES</code> 型の属性で指定された実体は、
非解析対象実体でなければいけません。

* VC:Fixed Attribute Default

layer=dtd
module=Web::XML::DTDValidator

@en
The value is different from the fixed default value
@ja
この値は固定既定値と異なります

@en
<p>If an attribute has the fixed default value, its value
must be equal to the default value.
@ja
<p>属性が固定既定値を持つなら、属性値と既定値は等しくなければいけません。

* VC:Attribute Value Type:declared

layer=dtd
module=Web::XML::DTDValidator

@en
The attribute is not declared
@ja
属性が宣言されていません

@en
<p>The attribute must be declared by an <code>ATTLIST</code> declaration.
@ja
<p>属性は <code>ATTLIST</code> 宣言で宣言しなければいけません。

* VC:Required Attribute

layer=dtd
module=Web::XML::DTDValidator

@en
The <code><var>{text}</var></code> attribute is not specified
@ja
<code><var>{text}</var></code> 属性が指定されていません

@en
<p>The required attribute must be specified.
@ja
<p>必須の属性は指定しなければいけません。

* VC:Element Valid:EMPTY

layer=dtd
module=Web::XML::DTDValidator

@en
The element is not empty
@ja
要素が空ではありません

@en
<p>The element whose content is <code>EMPTY</code> cannot contain
any child.
@ja
<p>内容が <code>EMPTY</code> の要素は子供を含めることができません。

* VC:Element Valid:mixed child

layer=dtd
module=Web::XML::DTDValidator

@en
The element is not allowed by the content model
@ja
内容モデルでこの要素は認められていません

@en
<p>The element declared with a mixed content can only contain
the listed elements as child.
@ja
<p>混合内容が宣言された要素の子供には指定された要素しか含められません。

* VC:Element Valid:element child:element

layer=dtd
module=Web::XML::DTDValidator

@en
The element is not one of allowed elements (<code><var>{text}</var></code>)
@ja
この要素は認められていません (<code><var>{text}</var></code>)

@en
<p>The element declared with an element content can only contain
the elements matching to the content model.
@ja
<p>要素内容と宣言された要素の内容としては内容モデルに合致する要素のみ使えます。

* VC:Element Valid:element child:required element

layer=dtd
module=Web::XML::DTDValidator

@en
There is no required element (<code><var>{text}</var></code>)
@ja
必須の要素がありません (<code><var>{text}</var></code>)

@en
<p>The element declared with an element content must contain
its required child elements.
@ja
<p>要素内容と宣言された要素の子供として内容モデルで要求された要素が必要です。
